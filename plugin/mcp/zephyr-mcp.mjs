#!/usr/bin/env node
import{existsSync as Y,readFileSync as Be,realpathSync as We,statSync as qe,writeFileSync as Vt}from"node:fs";import{dirname as ze,isAbsolute as Jt,join as P,relative as Xt,resolve as te,sep as Qt}from"node:path";import{DatabaseSync as en}from"node:sqlite";import{createHash as Ut}from"node:crypto";function ae(n,e){return n<e?-1:n>e?1:0}function Me(n){return(e,t)=>ae(n(e),n(t))}var ce=11,Le=2;function Gt(n){let e=t=>Array.isArray(t)?t.map(e):t!==null&&typeof t=="object"?Object.fromEntries(Object.entries(t).filter(([,r])=>r!==void 0).sort(([r],[o])=>ae(r,o)).map(([r,o])=>[r,e(o)])):t;return JSON.stringify(e(n))}function me(n){return Ut("sha256").update(Gt(n)).digest("hex")}function Fe(n){return me({projectRoot:n}).slice(0,24)}function Yt(n){let{zephyrRoot:e,projectRoot:t,producer:r,...o}=n;return me(o)}function He(n){let e;try{e=JSON.parse(n)}catch{throw new Error("the index descriptor is not valid JSON")}if(e===null||typeof e!="object"||Array.isArray(e))throw new Error("the index descriptor must be an object");let t=e,r=["builderVersion","createdAt","sourceKind","zephyrRoot","zephyrVersion","zephyrCommit","zephyrTreeFingerprint","moduleFingerprint","contextFingerprint"];for(let c of r)if(typeof t[c]!="string"||t[c]==="")throw new Error(`the index descriptor is missing ${c}`);if(!["pinned-upstream","west-workspace","explicit-tree"].includes(t.sourceKind))throw new Error(`the index descriptor has an invalid sourceKind: ${t.sourceKind}`);if(Number.isNaN(Date.parse(t.createdAt)))throw new Error("the index descriptor has an invalid createdAt timestamp");if(!/^[0-9a-f]{40}(?:[0-9a-f]{24})?$/.test(t.zephyrCommit))throw new Error("the index descriptor has an invalid Zephyr commit");for(let c of["zephyrTreeFingerprint","moduleFingerprint","contextFingerprint"])if(!/^[0-9a-f]{64}$/.test(t[c]))throw new Error(`the index descriptor has an invalid ${c}`);if(t.westManifestHash!==void 0&&!/^[0-9a-f]{64}$/.test(t.westManifestHash))throw new Error("the index descriptor has an invalid westManifestHash");if(t.descriptorVersion!==Le)throw new Error(`descriptor version ${String(t.descriptorVersion)} is incompatible; expected ${Le}`);if(t.schemaVersion!==ce)throw new Error(`schema version ${String(t.schemaVersion)} is incompatible; expected ${ce}`);if(t.coverage===null||typeof t.coverage!="object"||Array.isArray(t.coverage))throw new Error("the index descriptor has no coverage map");for(let[c,a]of Object.entries(t.coverage))if(a===null||typeof a!="object"||Array.isArray(a)||typeof a.complete!="boolean"||a.note!==void 0&&typeof a.note!="string")throw new Error(`the index descriptor has invalid coverage for ${c}`);let{createdAt:o,contextFingerprint:s,...i}=t;if(Yt(i)!==s)throw new Error("the index descriptor fingerprint is invalid");return t}function Pe(n){return{descriptorVersion:n.descriptorVersion,schemaVersion:n.schemaVersion,builderVersion:n.builderVersion,producer:n.producer??null,createdAt:n.createdAt,sourceKind:n.sourceKind,zephyrVersion:n.zephyrVersion,zephyrCommit:n.zephyrCommit,zephyrTreeFingerprint:n.zephyrTreeFingerprint,westManifestHash:n.westManifestHash??null,moduleFingerprint:n.moduleFingerprint,boardTarget:n.boardTarget??null,contextFingerprint:n.contextFingerprint,resolvedContext:!!(n.boardTarget||n.buildDirectory),coverage:n.coverage}}var B=class extends Error{};function Se(n,e,t){let r;try{r=qe(n,{bigint:!0})}catch{throw new B("The selected Zephyr index is unavailable. Rebuild it with the zephyr-index skill.")}if(!r.isFile())throw new B("The selected Zephyr index is not a regular file. Select or rebuild a valid index.");return{path:We(n),origin:e,identity:`${r.dev}:${r.ino}:${r.size}:${r.mtimeNs}`,...t?{projectRoot:t}:{}}}function tn(n){return n.ZEPHYR_AI_PLUGIN_DATA??n.CLAUDE_PLUGIN_DATA}function nn(n,e){let t=P(n,"indexes","projects",Fe(e)),r=P(t,"active.json");if(!Y(r))return null;let o;try{o=JSON.parse(Be(r,"utf8")).relativePath}catch{throw new B("The active project-index pointer is corrupt. Rebuild the project index.")}if(typeof o!="string"||Jt(o))throw new B("The active project-index pointer is invalid. Rebuild the project index.");let s=te(t,o),i=Xt(t,s);if(i===".."||i.startsWith(`..${Qt}`))throw new B("The active project-index pointer escapes its storage directory.");if(!Y(s))throw new B("The active project-index pointer names a missing artifact. Rebuild the project index.");return s}function Ze(n=process.env){let e=n.ZEPHYR_AI_INDEX;if(e){if(!Y(e))throw new B("ZEPHYR_AI_INDEX names a missing file. Correct it or rebuild the index.");return Se(te(e),"explicit")}let t=n.ZEPHYR_AI_PROJECT_ROOT??n.CLAUDE_PROJECT_DIR,r=t?te(t):te(process.cwd()),o=Y(r)?We(r):r,s=tn(n);if(s){let i=nn(te(s),o);if(i)return Se(i,"project",o)}if(!s){for(let i of[P(process.cwd(),"index","zephyr.db"),P(process.cwd(),"..","..","index","zephyr.db")])if(Y(i))return Se(te(i),"development",o)}return null}function Ke(n=process.cwd()){let e=te(n);for(let t=0;t<40;t++){if(Y(P(e,".west","config")))return e;let r=ze(e);if(r===e)break;e=r}return null}function Ue(n){try{let r=Be(P(n,".west","config"),"utf8").match(/^\s*path\s*=\s*(.+)$/m)?.[1]?.trim();if(r){let o=P(n,r);if(Y(P(o,"VERSION")))return o}}catch{}let e=P(n,"zephyr");return Y(P(e,"VERSION"))?e:null}var fe=class{db;info;meta;descriptor;constructor(e){this.info=e,this.db=new en(e.path,{readOnly:!0}),this.meta={};for(let o of this.db.prepare("SELECT key, value FROM meta").all())this.meta[String(o.key)]=String(o.value);let t=Number(this.meta.schema_version);if(t!==ce)throw this.db.close(),new Error(`Index schema ${Number.isFinite(t)?t:"unknown"} is incompatible; expected ${ce}. Rebuild the index.`);let r=this.meta.index_descriptor;if(!r)throw this.db.close(),new Error("The index has no descriptor. Rebuild it with the current indexer.");try{this.descriptor=He(r)}catch(o){throw this.db.close(),o}if(this.meta.context_fingerprint!==this.descriptor.contextFingerprint)throw this.db.close(),new Error("The index descriptor fingerprint does not match its metadata. Rebuild the index.");if(e.origin==="project")try{Vt(P(ze(e.path),"last-used"),`${new Date().toISOString()}
`)}catch{}}get sizeBytes(){try{return qe(this.info.path).size}catch{return 0}}all(e,...t){return this.db.prepare(e).all(...t)}get(e,...t){return this.db.prepare(e).get(...t)}search(e,t,r=[],o=50){let s=[],i=new Set;for(let c of rn(t))for(let a of this.all(e,c,...r)){let l=String(Object.values(a)[0]??"");if(!i.has(l)&&(i.add(l),s.push(a),s.length>=o))return s}return s}close(){this.db.close()}};function rn(n){let t=(n.match(/[\p{L}\p{N}_#.,:+-]+/gu)??[]).map(c=>c.replace(/^[.,:+-]+|[.,:+-]+$/g,"")).filter(c=>c.length>0).slice(0,12);if(t.length===0)return[];let r=c=>`"${c.replace(/"/g,'""')}"`,o=t.map(r),s=t.map(c=>`${r(c)}*`),i=[o.join(" AND ")];return s.join(" AND ")!==i[0]&&i.push(s.join(" AND ")),t.length>1&&i.push(s.join(" OR ")),i}function v(n,e){if(typeof n!="string"||n==="")return e;try{return JSON.parse(n)}catch{return e}}function A(n,e,t=50){let r=typeof n=="number"?n:Number.parseInt(String(n??""),10);return!Number.isFinite(r)||r<=0?e:Math.min(Math.floor(r),t)}function K(n,e){if(n.length<=e)return n;let t=n.slice(0,e),r=t.lastIndexOf(`
`);return`${(r>e*.5?t.slice(0,r):t).trimEnd()}
\u2026`}import{createInterface as on}from"node:readline";var Ge="2025-11-25",sn=["2025-11-25","2025-06-18","2025-03-26"],W={ParseError:-32700,InvalidRequest:-32600,MethodNotFound:-32601,InvalidParams:-32602,InternalError:-32603},E=class extends Error{};function z(n){return n==="$"?"arguments":`arguments${n.slice(1)}`}function ve(n,e,t="$"){let r=[],o=e.type;if(!(o===void 0||o==="object"&&n!==null&&typeof n=="object"&&!Array.isArray(n)||o==="array"&&Array.isArray(n)||o==="string"&&typeof n=="string"||o==="boolean"&&typeof n=="boolean"||o==="integer"&&typeof n=="number"&&Number.isInteger(n)||o==="number"&&typeof n=="number"&&Number.isFinite(n)))return[`${z(t)} must be ${String(o)}.`];if(Array.isArray(e.enum)&&!e.enum.some(i=>Object.is(i,n))&&r.push(`${z(t)} must be one of: ${e.enum.map(String).join(", ")}.`),typeof n=="number"&&(typeof e.minimum=="number"&&n<e.minimum&&r.push(`${z(t)} must be at least ${e.minimum}.`),typeof e.maximum=="number"&&n>e.maximum&&r.push(`${z(t)} must be at most ${e.maximum}.`)),typeof n=="string"&&(typeof e.minLength=="number"&&n.length<e.minLength&&r.push(`${z(t)} must contain at least ${e.minLength} characters.`),typeof e.maxLength=="number"&&n.length>e.maxLength&&r.push(`${z(t)} must contain at most ${e.maxLength} characters.`)),Array.isArray(n)){typeof e.minItems=="number"&&n.length<e.minItems&&r.push(`${z(t)} must contain at least ${e.minItems} item(s).`),typeof e.maxItems=="number"&&n.length>e.maxItems&&r.push(`${z(t)} must contain at most ${e.maxItems} item(s).`);let i=e.items;i&&typeof i=="object"&&!Array.isArray(i)&&n.forEach((c,a)=>r.push(...ve(c,i,`${t}[${a}]`)))}if(n!==null&&typeof n=="object"&&!Array.isArray(n)){let i=n,c=e.properties&&typeof e.properties=="object"&&!Array.isArray(e.properties)?e.properties:{},a=Array.isArray(e.required)?e.required:[];for(let l of a)typeof l=="string"&&!(l in i)&&r.push(`${z(`${t}.${l}`)} is required.`);if(e.additionalProperties===!1)for(let l of Object.keys(i))l in c||r.push(`${z(`${t}.${l}`)} is not allowed.`);for(let[l,d]of Object.entries(i)){let p=c[l];p&&r.push(...ve(d,p,`${t}.${l}`))}}return r}var V=class n{#r=new Map;#i=new Map;#o=new Map;#t;#e="new";#d=Ge;#c=!1;#u=!1;#h=0;#s=!1;#a=new Map;constructor(e){this.#t=e}tool(e){return this.#r.set(e.name,e),this}resource(e){return this.#i.set(e.uri,e),this}prompt(e){return this.#o.set(e.name,e),this}static log(e){process.stderr.write(`[zephyr-mcp] ${e}
`)}#l(e){process.stdout.write(`${JSON.stringify(e)}
`)}#m(e,t){this.#l({jsonrpc:"2.0",id:e,result:t})}#n(e,t,r,o){this.#l({jsonrpc:"2.0",id:e,error:o===void 0?{code:t,message:r}:{code:t,message:r,data:o}})}#p(){if(!this.#c||this.#e!=="ready"||this.#s)return;let e=`zephyr-roots-${++this.#h}`;this.#s=!0;let t=setTimeout(()=>{this.#a.delete(e)&&(this.#s=!1,n.log("roots/list timed out; continuing with the explicit project environment"))},5e3);t.unref(),this.#a.set(e,(r,o)=>{if(clearTimeout(t),this.#s=!1,o){n.log("the client rejected roots/list; continuing with the explicit project environment");return}let s=r&&typeof r=="object"&&Array.isArray(r.roots)?r.roots.flatMap(i=>i&&typeof i=="object"&&typeof i.uri=="string"?[String(i.uri)]:[]):[];this.#t.rootsChanged?.(s)}),this.#l({jsonrpc:"2.0",id:e,method:"roots/list",params:{}})}async#f(e,t){if(e==="initialize"){if(this.#e!=="new")throw new Z(W.InvalidRequest,"The server has already been initialized.");return this.#g(t)}if(e==="ping")return{};if(this.#e!=="ready")throw new Z(W.InvalidRequest,"The MCP initialization lifecycle is not complete.");switch(e){case"tools/list":return{tools:[...this.#r.values()].map(({handler:r,...o})=>o)};case"tools/call":return this.#b(t);case"resources/list":return{resources:[...this.#i.values()].map(({read:r,...o})=>o)};case"resources/templates/list":return{resourceTemplates:[]};case"resources/read":{let r=typeof t.uri=="string"?t.uri:"",o=this.#i.get(r);if(!o)throw new Z(W.InvalidParams,`Unknown resource: ${r}`);return{contents:[{uri:o.uri,mimeType:o.mimeType??"text/plain",text:await o.read()}]}}case"prompts/list":return{prompts:[...this.#o.values()].map(({render:r,...o})=>o)};case"prompts/get":{let r=typeof t.name=="string"?t.name:"",o=this.#o.get(r);if(!o)throw new Z(W.InvalidParams,`Unknown prompt: ${r}`);let s=t.arguments??{};return{...o.description?{description:o.description}:{},messages:[{role:"user",content:{type:"text",text:o.render(s)}}]}}case"logging/setLevel":return{};default:throw new Z(W.MethodNotFound,`Method not found: ${e}`)}}#g(e){let t=e.clientInfo;if(typeof e.protocolVersion!="string"||e.capabilities===null||typeof e.capabilities!="object"||Array.isArray(e.capabilities)||t===null||typeof t!="object"||Array.isArray(t)||typeof t.name!="string"||typeof t.version!="string")throw new Z(W.InvalidParams,"initialize requires protocolVersion, capabilities, and clientInfo with name and version.");let r=typeof e.protocolVersion=="string"?e.protocolVersion:"";this.#d=sn.includes(r)?r:Ge,this.#e="initializing";let s=e.capabilities.roots;this.#c=s!==null&&typeof s=="object"&&!Array.isArray(s),this.#u=this.#c&&s.listChanged===!0;let i={};return this.#r.size>0&&(i.tools={listChanged:!1}),this.#i.size>0&&(i.resources={listChanged:!1,subscribe:!1}),this.#o.size>0&&(i.prompts={listChanged:!1}),{protocolVersion:this.#d,capabilities:i,serverInfo:{name:this.#t.name,version:this.#t.version,...this.#t.title?{title:this.#t.title}:{},...this.#t.description?{description:this.#t.description}:{}},...this.#t.instructions?{instructions:this.#t.instructions}:{}}}async#b(e){let t=typeof e.name=="string"?e.name:"",r=this.#r.get(t);if(!r)throw new Z(W.InvalidParams,`Unknown tool: ${t}`);let o=e.arguments??{};try{let s=ve(o,r.inputSchema);if(s.length>0)throw new E(`Invalid input for ${t}:
${s.map(c=>`- ${c}`).join(`
`)}`);let i=await r.handler(o);return r.outputSchema&&i.structuredContent&&i.content.length===0&&(i.content=[{type:"text",text:JSON.stringify(i.structuredContent,null,2)}]),i}catch(s){if(s instanceof E)return{content:[{type:"text",text:s.message}],isError:!0};let i=s instanceof Error?s.stack??s.message:String(s);return n.log(`tool ${t} failed: ${i}`),{content:[{type:"text",text:`The ${t} lookup failed because the local index could not be read. Run index_status and rebuild the index if needed.`}],isError:!0}}}start(){let e=on({input:process.stdin,crlfDelay:1/0});e.on("line",t=>{let r=t.trim();if(r==="")return;let o;try{o=JSON.parse(r)}catch{this.#n(null,W.ParseError,"Parse error");return}if(typeof o!="object"||o===null||Array.isArray(o)){this.#n(null,W.InvalidRequest,"Invalid Request");return}let s=o,i=s.id,c=typeof i=="string"||typeof i=="number"&&Number.isFinite(i)?i:null,a="result"in s,l="error"in s;if(!("method"in s)&&"id"in s&&(a||l)){let u=a!==l&&(typeof i=="string"||typeof i=="number"&&Number.isFinite(i))?this.#a.get(i):void 0;if(!u){n.log("ignored a malformed or unmatched response to a server-originated request");return}this.#a.delete(i),u(s.result,s.error);return}if(s.jsonrpc!=="2.0"||typeof s.method!="string"||s.method===""||i!=null&&c===null||"params"in s&&(s.params===null||typeof s.params!="object"||Array.isArray(s.params))){this.#n(c,W.InvalidRequest,"Invalid Request");return}let d=c;if(!("id"in s)){o.method==="notifications/initialized"&&this.#e==="initializing"?(this.#e="ready",this.#p()):o.method==="notifications/roots/list_changed"&&this.#e==="ready"&&this.#u&&this.#p();return}this.#f(o.method,o.params??{}).then(u=>this.#m(d,u)).catch(u=>{if(u instanceof Z){this.#n(d,u.code,u.message);return}let h=u instanceof Error?u.stack??u.message:String(u);n.log(`internal error handling ${o.method}: ${h}`),this.#n(d,W.InternalError,"Internal server error")})}),e.on("close",()=>{this.#e!=="closing"&&(this.#e="closing"),this.#e="closed",process.exit(0)}),process.once("SIGTERM",()=>{this.#e="closing",e.close()})}get initialized(){return this.#e==="ready"}},Z=class extends Error{constructor(t,r){super(r);this.code=t}code};function I(n,e){return{content:[{type:"text",text:n}],...e?{structuredContent:e}:{}}}function j(n,e){let t=n[e];if(typeof t!="string"||t.trim()==="")throw new E(`The "${e}" argument is required and must be a non-empty string.`);return t.trim()}function k(n,e){let t=n[e];return typeof t=="string"&&t.trim()!==""?t.trim():void 0}function L(n,e,t){return I(`No ${n} matched "${e}".

${t}`,{results:[],query:e})}function Ye(n,e,t,r,o=3){for(let s=Math.min(t.length,24);s>=o;s--){let i=n(e,`${t.slice(0,s).replace(/[%_]/g,"\\$&")}%`);if(i.length>0)return i.map(c=>String(c[r]))}return[]}function le(n,e){let t=Array.from({length:e.length+1},(r,o)=>o);for(let r=1;r<=n.length;r++){let o=t[0];t[0]=r;for(let s=1;s<=e.length;s++){let i=t[s];t[s]=Math.min(t[s]+1,t[s-1]+1,o+(n[r-1]===e[s-1]?0:1)),o=i}}return t[e.length]}function an(n,e){let t=s=>s.replace(/^CONFIG_/,"").toLowerCase().replace(/[^a-z0-9]/g,""),r=le,o=t(n);return o.length<3?[]:e.filter(s=>{let i=t(s);if(i.length<3)return!1;let c=r(o,i),a=Math.max(o.length,i.length);return c<=2&&1-c/a>=.72})}function U(n,e,t,r=[],o="Generated, application-local, and external-module declarations may not be covered."){let s=an(e,r);return`${n} "${e}" was not found in the indexed Zephyr ${t} catalogue. `+o+(s.length>0?`

Close spelling matches:
${s.map(i=>`- \`${i}\``).join(`
`)}`:`

No sufficiently close spelling match was found.`)}function ie(n,e,t,r=[],o="Generated, application-local, and external-module declarations may not be covered."){throw new E(U(n,e,t,r,o))}function J(n,e,t){let r=n[t];if(r!==void 0){if(n[e]!==void 0)throw new E(`Supply "${e}" or "${t}", not both.`);let o=r.map(s=>typeof s=="string"?s.trim():"").filter(s=>s!=="");if(o.length===0)throw new E(`"${t}" must contain at least one non-empty name.`);return{values:[...new Set(o)],batched:!0}}return{values:[j(n,e)],batched:!1}}function X(n,e){let t=[],r=[],o=0;for(let i of n){if(t.length>0&&o+i.text.length>e){r.push(i.key);continue}t.push(i),o+=i.text.length}let s=O([...t.map(i=>i.text),r.length>0?`_Requested but not rendered, to stay within ${e} characters: ${r.map(i=>`\`${i}\``).join(", ")}. Ask for them in a smaller batch._`:void 0]);return I(s,{requested:n.length,rendered:t.length,omitted:r,results:n.map(i=>i.structured)})}var cn=50,Q=4e4;function ee(n){return{type:"array",items:{type:"string"},minItems:1,maxItems:cn,description:n}}function ge(n){return n.endsWith(".c")||n.endsWith(".h")?"c":n.endsWith(".cpp")||n.endsWith(".hpp")?"cpp":n.endsWith(".overlay")||n.endsWith(".dts")||n.endsWith(".dtsi")?"dts":n.endsWith(".conf")||n.endsWith("_defconfig")?"kconfig":n.endsWith("CMakeLists.txt")||n.endsWith(".cmake")?"cmake":n.endsWith(".yaml")||n.endsWith(".yml")?"yaml":n.endsWith(".py")?"python":n.endsWith(".sh")?"bash":n.endsWith(".rst")?"rst":(n.endsWith(".ld")||n.endsWith(".S")||n.endsWith(".s"),"")}function G(n,e){let t=n.slice(0,e).map(o=>`\`${o}\``).join(", "),r=n.length-e;return r>0?`${t} \u2014 and ${r} more, ${n.length} in total`:t}var Ve={type:"string"};function F(n,e=50){return{type:"integer",minimum:1,maximum:e,default:n,description:`Maximum results to return (default ${n}, max ${e}).`}}function R(n,e){let t=e.filter(r=>r.trim()!=="");return t.length===0?"":`**${n}**
${t.map(r=>`- ${r}`).join(`
`)}`}function O(n){return n.filter(e=>!!(e&&e.trim())).join(`

`)}var Je=["function","macro","struct","enum","enumvalue","typedef","union","variable"],Xe=n=>({name:"search_api",title:"Search the Zephyr C API",description:'Search Zephyr public C functions, macros, structs, enums, and typedefs by name or purpose. Use this to confirm a function exists and to find the right one for a task ("read a sensor channel", "get a device from devicetree", "submit work"). Zephyr often has more than one API for the same job, such as device_get_binding() and DEVICE_DT_GET(), or sensor fetch-and-get and read-and-decode. Search first so you do not pick a deprecated one.',inputSchema:{type:"object",properties:{query:{type:"string",description:"Symbol name fragment or description of the task."},kind:{type:"string",enum:Je,description:"Restrict to one kind of symbol."},group:{type:"string",description:'Restrict to a Doxygen API group, e.g. "gpio_interface", "thread_apis".'},limit:F(12)},required:["query"],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=j(e,"query"),r=k(e,"kind"),o=k(e,"group"),s=A(e.limit,12),i=[],c=[];r&&(i.push("AND s.kind = ?"),c.push(r)),o&&(i.push("AND s.api_group = ?"),c.push(o));let a=n().search(`SELECT s.name, s.kind, s.signature, s.brief, s.header, s.api_group, s.deprecated
         FROM api_fts f JOIN api_symbol s ON s.id = f.rowid
        WHERE api_fts MATCH ? ${i.join(" ")}
        ORDER BY bm25(api_fts, 10.0, 3.0, 1.0)
        LIMIT ?`,t,[...c,s],s);if(a.length===0)return L("API symbols",t,'Try the subsystem prefix on its own ("k_", "gpio_", "sensor_"), or search_docs for the concept. Driver-internal and vendor HAL functions are not indexed.');let l=a.map(p=>({name:String(p.name),kind:String(p.kind),signature:String(p.signature??""),brief:String(p.brief??""),header:String(p.header),group:p.api_group??null,deprecated:Number(p.deprecated)===1})),d=l.map(p=>[`### ${p.name}${p.deprecated?"  \u26A0\uFE0F DEPRECATED":""}`,"```c",K(p.signature,300),"```",p.brief,`_${p.kind} \xB7 ${p.header}${p.group?` \xB7 group ${p.group}`:""}_`].filter(Boolean).join(`
`)).join(`

`);return I(`Found ${l.length} symbol(s) for "${t}".

${d}

Use get_api for parameters and return values.`,{query:t,count:l.length,results:l})}});function Qe(n,e){let t=n.search(`SELECT s.name, s.kind FROM api_fts f JOIN api_symbol s ON s.id = f.rowid
      WHERE api_fts MATCH ? ORDER BY bm25(api_fts, 10.0, 3.0, 1.0) LIMIT 8`,e.replace(/_/g," "),[],8);return U("C API symbol",e,n.meta.zephyr_version??"unknown",t.map(r=>String(r.name)),"Internal APIs, vendor-native SDK APIs, generated declarations, and external-module headers may not be covered.")}function ln(n,e,t){let r=e.replace(/\(\)$/,""),o=n.all(`SELECT name, kind, signature, brief, header, line, deprecated
       FROM api_symbol WHERE name = ? ${t?"AND kind = ?":""}
       ORDER BY CASE kind WHEN 'function' THEN 0 WHEN 'macro' THEN 1 ELSE 2 END`,...t?[r,t]:[r]);if(o.length===0)return{key:r,text:`### ${r}

${Qe(n,r)}`,structured:{name:r,found:!1}};let s=o[0],i=String(s.kind)==="enum"?Number(n.get(`SELECT COUNT(*) AS c FROM api_symbol
              WHERE kind = 'enumvalue' AND parent_symbol = ? AND header = ?`,String(s.name),String(s.header))?.c??0):0;return{key:r,text:O([`### ${r}${Number(s.deprecated)===1?"  \u26A0\uFE0F DEPRECATED":""}`,`\`\`\`c
${String(s.signature)}
\`\`\``,s.brief?String(s.brief):"_No brief description is present in the indexed API source._",i>0?`_${i} enum members \u2014 call get_api with a single name to list them._`:void 0,`_${String(s.kind)} \xB7 \`${String(s.header)}:${Number(s.line)}\`${o.length>1?` \xB7 also defined as ${o.slice(1).map(c=>String(c.kind)).join(", ")}`:""}_`]),structured:{name:s.name,found:!0,kind:s.kind,signature:s.signature,brief:s.brief??"",header:s.header,line:s.line,deprecated:Number(s.deprecated)===1,memberCount:i}}}var et=n=>({name:"get_api",title:"Get Zephyr API symbols",description:'Get the indexed contract of a Zephyr C symbol: signature, parameter documentation, return description, and documented error codes. Use before calling an unfamiliar function. Zephyr commonly returns negative errno values, and the documented set differs per function. When a symbol has no documentation the tool reports that as unknown; it does not mean the call cannot fail. Pass "names" to check several symbols and read their signatures in one call.',inputSchema:{type:"object",properties:{name:{type:"string",description:'Exact symbol name, e.g. "gpio_pin_configure_dt".'},names:ee('Several symbols in one call, e.g. ["sensor_sample_fetch", "sensor_channel_get"]. Returns each signature and where it is declared, without the parameter and return documentation the single-name form gives.'),kind:{type:"string",enum:Je,description:"Disambiguate when a name exists as more than one kind."}},additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let{values:t,batched:r}=J(e,"name","names"),o=k(e,"kind"),s=n();if(r)return X(t.map(f=>ln(s,f,o)),Q);let i=t[0].replace(/\(\)$/,""),c=s.all(`SELECT name, kind, signature, brief, detail, params, returns, retvals,
              api_group, since, deprecated, header, line, doxygen_id, doc_anchor
         FROM api_symbol WHERE name = ? ${o?"AND kind = ?":""}
         ORDER BY CASE kind WHEN 'function' THEN 0 WHEN 'macro' THEN 1 ELSE 2 END`,...o?[i,o]:[i]);if(c.length===0)throw new E(Qe(s,i));let a=c[0],l=v(a.params,[]),d=v(a.returns,[]),p=v(a.retvals,[]),u=String(a.kind)==="enum"?s.all(`SELECT name, signature, brief FROM api_symbol
                WHERE kind = 'enumvalue' AND parent_symbol = ? AND header = ? ORDER BY id`,String(a.name),String(a.header)).map(f=>({name:String(f.name),signature:String(f.signature??""),brief:String(f.brief??"")})):[],h=O([`# ${String(a.name)}${Number(a.deprecated)===1?"  \u26A0\uFE0F DEPRECATED":""}`,`\`\`\`c
${String(a.signature)}
\`\`\``,a.brief?String(a.brief):"_No brief description is present in the indexed API source._",a.detail?String(a.detail):"_No detailed documentation is present; this does not imply the API has no constraints or failure modes._",R("Members",u.map(f=>`\`${f.signature||f.name}\`${f.brief?` \u2014 ${f.brief}`:""}`)),String(a.kind)==="enum"&&u.length===0?"_No members are recorded for this enum in the indexed API source._":void 0,R("Parameters",l.map(f=>`\`${f.name}\`${f.type?` (${f.type})`:""}${f.direction?` *[${f.direction}]*`:""} \u2014 `+(f.description||"_undocumented_"))),d.length>0?R("Returns",d):"_No return description is present in the indexed API source._",p.length>0?R("Return values",p.map(f=>`\`${f.value}\` \u2014 ${f.description}`)):"_No individual return values are documented here; this does not prove the call cannot fail._",a.since?`_Available since Zephyr ${String(a.since)}._`:void 0,a.doc_anchor?`[Official API documentation](${s.meta.doc_base_url??""}doxygen/html/${String(a.doc_anchor)})`:"_No stable Doxygen documentation anchor is available because this index used the header fallback._",`_${String(a.kind)} \xB7 \`${String(a.header)}:${Number(a.line)}\`${a.api_group?` \xB7 group \`${String(a.api_group)}\``:""}_`,c.length>1?`_Also defined as: ${c.slice(1).map(f=>String(f.kind)).join(", ")}. Pass "kind" to select one._`:void 0]);return I(h,{name:a.name,found:!0,kind:a.kind,signature:a.signature,brief:a.brief??"",detail:a.detail??"",params:l,returns:d,retvals:p,members:u,group:a.api_group??null,header:a.header,line:a.line,deprecated:Number(a.deprecated)===1,since:a.since??null,doxygenId:a.doxygen_id??null,documentationUrl:a.doc_anchor?`${s.meta.doc_base_url??""}doxygen/html/${String(a.doc_anchor)}`:null})}});function dn(n){let[e="",...t]=n.split("/"),r=e.indexOf("@");return{board:r<0?e:e.slice(0,r),revision:r<0?"":e.slice(r+1),qualifiers:t.filter(Boolean)}}function un(n){let{board:e,revision:t,qualifiers:r}=dn(n);if(!e)return[];let o=[],s=i=>{i&&!o.includes(i)&&o.push(i)};s(e);for(let i=1;i<=r.length;i++)s([e,...r.slice(0,i)].join("_"));if(t){let i=`${e}_${t.replace(/\./g,"_")}`;s(i);for(let c=1;c<=r.length;c++)s([i,...r.slice(0,c)].join("_"))}return o}function tt(n){let e=un(n);return{overlay:e.map(t=>`${t}.overlay`),conf:e.map(t=>`${t}.conf`),defconfig:e.map(t=>`${t}_defconfig`)}}function nt(n){return[...new Set(n.split(/[\s()[\],/]+/).map(e=>e.replace(/[.,;:]+$/,"").toUpperCase()).filter(e=>e.length>=6&&/\d/.test(e)&&/[A-Z]/.test(e)))]}function pn(n,e,t,r,o,s){let i=nt(t);if(r.length===0)return[];let c=new Set(r.map(d=>d.name)),a=n.all(`SELECT DISTINCT b.name, b.full_name, b.ram, b.flash, s.name AS soc
       FROM board b, json_each(b.socs) j
       JOIN soc s ON s.name = json_extract(j.value, '$.name')
      WHERE b.name <> ?
        AND b.vendor = (SELECT vendor FROM board WHERE name = ?)
        AND s.series IN (
          SELECT series FROM soc WHERE name IN (
            SELECT json_extract(value, '$.name') FROM json_each((SELECT socs FROM board WHERE name = ?))
          )
        )`,e,e,e),l=[];for(let d of a){let p=String(d.full_name??""),u=String(d.soc),h=c.has(u),f=null;for(let $ of i)for(let m of nt(p)){if(Math.abs($.length-m.length)>2)continue;let b=le($,m);b===0||b>2||b>1&&1-b/Math.max($.length,m.length)<.85||(f===null||b<f)&&(f=b)}if(f===null&&!h)continue;let w=d.ram===null?null:Number(d.ram),g=d.flash===null?null:Number(d.flash);l.push({name:String(d.name),fullName:p,soc:u,ram:w,flash:g,relation:h?"same SoC":"similar name",differs:[h?"":`SoC ${u}`,w!==null&&w!==o?`${w} KB RAM`:"",g!==null&&g!==s?`${g} KB flash`:""].filter(Boolean)})}return l.sort(Me(d=>`${d.relation==="same SoC"?"0":"1"} ${d.name}`))}function hn(n,e){return n.all(`SELECT r.runner, r.available, r.flash_default, r.debug_default, r.args, r.declared_in
         FROM board_runner r JOIN board b ON b.id = r.board_id
        WHERE b.name = ? ORDER BY r.runner`,e).map(t=>({runner:String(t.runner),available:Number(t.available)===1,flashDefault:Number(t.flash_default)===1,debugDefault:Number(t.debug_default)===1,args:v(t.args,[]),declaredIn:v(t.declared_in,[])}))}function mn(n,e){if(n.length===0)return"**Flashing and debugging**\nThis board declares no runner in its `board.cmake`. That is what the tree says, not proof that the board cannot be programmed \u2014 emulated targets run with `west build -t run`, and some boards are programmed by vendor tooling outside west.";let t=n.filter(a=>a.available),r=n.find(a=>a.flashDefault),o=n.find(a=>a.debugDefault),s=n.map(a=>{let l=[a.flashDefault?"`west flash` default":"",a.debugDefault?"`west debug` default":""].filter(Boolean),d=a.args.filter(u=>u.guard).length,p=[l.join(", "),a.available?"":"declared but never registered \u2014 this command will fail",a.args.length>0?`${a.args.length} preset argument(s)`:"",d>0?`${d} of them conditional on Kconfig`:""].filter(Boolean).join(" \xB7 ");return`\`${a.runner}\`${p?` \u2014 ${p}`:""}`}),i=[r?.available?`\`west flash\` uses \`${r.runner}\``:"",o?.available?`\`west debug\` uses \`${o.runner}\``:""].filter(Boolean),c=[r,o].filter((a,l,d)=>!!a?.available&&d.indexOf(a)===l).flatMap(a=>a.args.length===0?[]:a.args.some(l=>l.guard||l.unresolved)?[]:[`\`${a.runner}\`: ${a.args.map(l=>`\`${l.value}\``).join(" ")}`]);return O([R(`Flashing and debugging \`${e}\``,s),i.length>0?i.join("; ")+".":void 0,c.length>0?R("Arguments the board always passes",c)+"\n\nConditional and build-time-expanded arguments are not shown; `west flash --context` resolves them against a configured build.":void 0,t.length>1?"Select another with `west flash -r <runner>`. Call get_runner for what each accepts.":void 0,t.length>1||o?.available&&r?.available&&o.runner!==r.runner?"If loading an image onto this board requires a manual step such as moving a jumper or power-cycling, compare these runners before you start iterating. They differ in whether they program flash, load through the debug probe, or use a separate boot path, and `west debug` often loads without programming flash. Call get_runner for what each one accepts. The board documentation and `board.cmake` list alternate boot modes that are not runners.":void 0,o&&!o.available?`\`${o.runner}\` is set as the debug default but is not registered for this board, so \`west debug\` has no runner to use.`:void 0])}function fn(n,e){let t;try{t=n.all(`SELECT s.path AS suite, s.kind AS kind, f.path AS file, f.target AS target, f.kind AS role
         FROM sample_board_file f JOIN sample s ON s.id = f.sample_id
        WHERE f.board = ?
        ORDER BY f.target, s.path, f.path`,e)}catch{return{structured:[]}}if(t.length===0)return{structured:[]};let r=t.map(s=>({suite:String(s.suite),kind:String(s.kind),file:String(s.file),target:String(s.target??""),role:String(s.role)})),o=r.slice(0,12).map(s=>`\`${s.suite}\` ships \`${s.file}\``+(s.target?` for \`${s.target}\``:" for every target of this board"));return{text:R(`Upstream configures this board in ${r.length} place(s)`,o)+(r.length>12?`
- \u2026and ${r.length-12} more`:"")+"\n\nThese are the vendor's own settings for this exact target, including the DMA channels, clock sources and cache attributes a peripheral needs here. Read one with get_sample before writing your own. They are invisible to a search on Twister metadata, because a suite can ship a file for a board without naming it in `platform_allow` or `integration_platforms`.",structured:r}}function gn(n,e){let t;try{t=n.all("SELECT target, role, label, node, address, size, source FROM board_memory WHERE board = ? ORDER BY target, role",e)}catch{return{structured:[]}}if(t.length===0)return{structured:[]};let r=t.map(c=>({target:String(c.target??""),role:String(c.role),label:String(c.label),node:String(c.node),address:Number(c.address),size:Number(c.size),source:String(c.source)})),o=c=>c>=1024*1024&&c%(1024*1024)===0?`${c/(1024*1024)} MB`:c>=1024?`${Math.round(c/1024*10)/10} KB`:`${c} bytes`,s=c=>c.role==="sram"?"application RAM":c.role==="code-partition"?"code partition":"flash",i=r.map(c=>`${s(c)}${c.target?` on \`${c.target}\``:""}: **${o(c.size)}** at \`0x${c.address.toString(16)}\` from \`${c.label}\``+(c.role==="code-partition"?" (an offset within its flash, not an absolute address)":"")+` \u2014 declared in \`${c.source}\``);return{text:R("Memory the application actually gets",i)+"\n\nRead from this board's `chosen` node and the `reg` of what it points at, which is what the linker uses. Where a board declares nothing this index could resolve, no figure is given rather than a guessed one.",structured:r}}var rt=n=>({name:"search_boards",title:"Search boards",description:"Find Zephyr boards by name, vendor, SoC, or supported peripheral. Returns the qualified board target identifier that `west build -b` expects. That is often not the bare board name: the ESP32-S3 DevKitC builds as `esp32s3_devkitc/esp32s3/procpu`, and `esp32s3_devkitc` alone fails. Also use this to check whether a board supports a peripheral before designing around it.",inputSchema:{type:"object",properties:{query:{type:"string",description:'Board name, product name, SoC, or vendor, e.g. "nucleo h743" or "esp32s3".'},vendor:{type:"string",description:'Restrict to a vendor, e.g. "st", "espressif".'},arch:{type:"string",description:"Restrict to an architecture: arm, xtensa, riscv, ..."},feature:{type:"string",description:'Require support for a peripheral tag, e.g. "can", "netif:wifi", "usb_device", "adc".'},runner:{type:"string",description:'Require that the board registers this flash/debug runner, e.g. "pyocd", "jlink". Use when the probe on the desk decides which board is worth considering.'},limit:F(15)},required:["query"],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=j(e,"query"),r=k(e,"vendor"),o=k(e,"arch"),s=k(e,"feature"),i=k(e,"runner"),c=A(e.limit,15),a=[],l=[];r&&(a.push("AND b.vendor = ?"),l.push(r)),o&&(a.push("AND b.arch = ?"),l.push(o)),s&&(a.push("AND (' ' || b.supported_text || ' ') LIKE ?"),l.push(`% ${s} %`)),i&&(a.push("AND EXISTS (SELECT 1 FROM board_runner br WHERE br.board_id = b.id AND br.runner = ? AND br.available = 1)"),l.push(i));let d=n().search(`SELECT b.name, b.full_name, b.vendor, b.arch, b.ram, b.flash,
              b.targets_text, b.socs_text, b.supported_text, b.doc_path
         FROM board_fts f JOIN board b ON b.id = f.rowid
        WHERE board_fts MATCH ? ${a.join(" ")}
        ORDER BY bm25(board_fts, 10.0, 6.0, 2.0, 4.0, 1.0, 3.0)
        LIMIT ?`,t,[...l,c],c);if(d.length===0)return L("boards",t,"Try the vendor name, the SoC part number, or a shorter fragment. Boards from out-of-tree HAL modules are not in this index.");let p=d.map(h=>({name:String(h.name),fullName:String(h.full_name??""),vendor:String(h.vendor??""),arch:h.arch??null,targets:String(h.targets_text??"").split(" ").filter(Boolean),socs:String(h.socs_text??"").split(" ").filter(Boolean),supported:String(h.supported_text??"").split(" ").filter(Boolean),ramKb:h.ram??null,flashKb:h.flash??null})),u=p.map(h=>{let f=h.targets.length>0?h.targets:[h.name],w=[h.flashKb?`${h.flashKb} KB flash`:"",h.ramKb?`${h.ramKb} KB RAM`:""].filter(Boolean).join(", ");return[`### ${h.fullName||h.name}`,`**Build targets:** ${f.map(g=>`\`${g}\``).join(" , ")}`,`vendor: ${h.vendor}${h.arch?` \xB7 arch: ${h.arch}`:""}${h.socs.length?` \xB7 SoC: ${h.socs.join(", ")}`:""}${w?` \xB7 ${w}`:""}`,h.supported.length>0?`supports: ${h.supported.join(", ")}`:""].filter(Boolean).join(`
`)}).join(`

`);return I(`Found ${p.length} board(s) for "${t}".

${u}

Pass a build target to \`west build -b <target>\`. Use get_board for pin and flashing detail.`,{query:t,count:p.length,results:p})}}),it=n=>({name:"get_board",title:"Get a board",description:"Get what the index holds about one Zephyr board: every qualified build target with its architecture, Twister flash and RAM figures and supported peripherals; the memory the application actually gets, read from the board's own chosen node rather than from test metadata; the overlay and .conf filenames each target picks up; which upstream samples and tests ship a configuration file for this board's targets, which is where the vendor's own DMA, clock and cache settings for each peripheral are; the SoCs and CPU clusters it contains; hardware revisions; the runners it registers and which one west flash and west debug select; and a link to its documentation page. Use before starting a project for a board, before writing a peripheral overlay for one, and to confirm which peripherals it exposes. It also lists boards this one is easy to confuse with, either because they carry the same SoC or because the names are close, with the figures that differ.",inputSchema:{type:"object",properties:{name:{type:"string",description:'Board name (e.g. "nucleo_h743zi"). A qualified target is also accepted.'},board:{type:"string",description:"Alias for name, accepted because it matches the get_board tool name."}},anyOf:[{required:["name"]},{required:["board"]}],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=k(e,"name"),r=k(e,"board");if(t&&r&&t!==r)throw new E('The "name" and "board" arguments must match when both are supplied.');let o=t??r;if(!o)throw new E('The "name" argument (or its "board" alias) is required.');let s=o.split("/")[0].split("@")[0].trim(),i=n(),c=i.get(`SELECT name, full_name, vendor, dir, arch, ram, flash, socs, targets,
              revisions, default_revision, supported, doc_path, no_xip_targets
         FROM board WHERE name = ?`,s);if(!c){let S=i.search(`SELECT b.name FROM board_fts f JOIN board b ON b.id = f.rowid
          WHERE board_fts MATCH ? ORDER BY bm25(board_fts) LIMIT 8`,s.replace(/[_/]/g," "),[],8);return ie("Board",s,i.meta.zephyr_version??"unknown",S.map(N=>String(N.name)),"Boards supplied by external modules or workspace-specific hardware roots may not be covered.")}let a=v(c.targets,[]),l=v(c.socs,[]),d=v(c.revisions,[]),p=v(c.supported,[]),u=c.ram===null?null:Number(c.ram),h=c.flash===null?null:Number(c.flash),f=c.doc_path,w=l.map(S=>{let N=i.get("SELECT series, family, vendor FROM soc WHERE name = ?",S.name);return{...S,series:N?String(N.series??""):"",family:N?String(N.family??""):""}}),g=pn(i,s,String(c.full_name??""),l,u,h),$=hn(i,s),m=fn(i,s),b=gn(i,s),y=f?`${(i.meta.doc_base_url??"").replace(/\/?$/,"/")}${f.replace(/\.rst$/,".html")}`:null,T=new Set(v(c.no_xip_targets,[])),x=a.map(S=>{let N=[S.flash?`${S.flash} KB flash`:"",S.ram?`${S.ram} KB RAM`:""].filter(Boolean).join(", "),H=[S.arch,N,S.toolchains.length?`toolchains: ${S.toolchains.join(", ")}`:""].filter(Boolean).join(" \xB7 "),q=tt(S.identifier),se=q.overlay[q.overlay.length-1]??"",pe=q.conf[q.conf.length-1]??"";return`\`${S.identifier}\`${H?` \u2014 ${H}`:""}`+(T.has(S.identifier)?"\n  - `CONFIG_XIP=n` in this target's defconfig":"")+(se?`
  - overlay: \`boards/${se}\``:"")+(pe?`
  - conf:    \`boards/${pe}\``:"")}),_=O([`# ${String(c.full_name||c.name)}`,`Board \`${String(c.name)}\` from **${String(c.vendor??"unknown")}**${c.arch?`, ${String(c.arch)} architecture`:""}${h!==null?`, ${h} KB flash`:""}${u!==null?`, ${u} KB RAM`:""}.`,h!==null||u!==null?"_Those figures come from `twister.yaml`. They are test metadata, not the memory the application gets."+(T.size>0?" Some targets on this board set `CONFIG_XIP=n`, so the image is not executed from an internal flash that the flash figure could describe. Use the devicetree partitions for the actual limit.":"")+"_":void 0,b.text,R("Build targets (use with `west build -b`)",x),a.length>0?"_Zephyr selects board overlays and `.conf` fragments by building these filenames and testing whether the file exists, so a file named for the unqualified board does not apply to a qualified target. A file that matches nothing produces no warning. Check the configure output for `Found devicetree overlay:` and `Merged configuration`, and check it again after changing the target, not only after changing the file._":void 0,R("SoCs",w.map(S=>`\`${S.name}\``+(S.series?` \u2014 series \`${S.series}\``:"")+(S.family?`, family \`${S.family}\``:"")+(S.variants.length?` \u2014 variants: ${S.variants.join(", ")}`:"")+(S.cpuclusters.length?` \u2014 CPU clusters: ${S.cpuclusters.join(", ")}`:""))),g.length>0?R("Easily confused with",g.slice(0,8).map(S=>`\`${S.name}\` (${S.fullName}) \u2014 ${S.relation}`+(S.differs.length>0?`; differs: ${S.differs.join(", ")}`:"; same flash and RAM figures \u2014 check the peripheral list, the external memory part, and the runner arguments")))+(g.length>8?`
- \u2026and ${g.length-8} more`:"")+`

Each entry is listed either because it uses the same SoC or because its name is close to this one. Same-SoC boards are easy to miss, because vendor names do not show the relation. What differs between them is usually the external flash part, the loader file, the download address, and the memory figures, and each of those breaks a build. Check the target against the SoC rather than against a datasheet.`:void 0,m.text,mn($,s),p.length>0?`**Supported peripherals** (${p.length})
${p.map(S=>`\`${S}\``).join(" \xB7 ")}`:void 0,d.length>0?`**Hardware revisions**: ${d.map(S=>`\`${S}\``).join(", ")}`+(c.default_revision?` (default \`${String(c.default_revision)}\`)`:"")+"\nSelect one with `west build -b "+s+"@<revision>`.":void 0,y?`**Documentation** (pinout, jumpers, flashing): ${y}
Source: \`${f}\` \u2014 read it with get_doc.`:void 0,`_Board directory: \`${String(c.dir)}\`_`]);return I(_,{name:String(c.name),found:!0,fullName:c.full_name??"",vendor:c.vendor??"",arch:c.arch??null,ram:u,flash:h,dir:c.dir,targets:a,socs:w,runners:$,memory:b.structured,upstreamConfiguration:m.structured,nearTwins:g,revisions:d,defaultRevision:c.default_revision??null,supported:p,docPath:f,docUrl:y})}});var st=new Set(["compatible","status","interrupt-parent","interrupts-extended","wakeup-source","zephyr,deferred-init","zephyr,pm-device-runtime-auto","zephyr,disabling-power-states","power-domains","power-domain-names","#power-domain-cells","label","hwlocks","hwlock-names","mbox-names","mboxes"]);function Ee(n){let e=[`\`${n.name}\``];n.type&&e.push(`*${n.type}*`),n.required&&e.push("**required**"),n.deprecated&&e.push("_deprecated_");let t=[];n.const_value!==null&&t.push(`const ${n.const_value}`),n.default_value!==null&&t.push(`default ${n.default_value}`);let r=v(n.enum_values,[]),o=Array.isArray(r)?r:[];o.length>0&&t.push(`one of ${o.map(d=>JSON.stringify(d)).join(", ")}`),n.specifier_space&&t.push(`specifier-space ${n.specifier_space}`);let s=v(n.constraints,{});for(let[d,p]of Object.entries(s))t.push(`${d} ${Array.isArray(p)?p.map(String).join(", "):JSON.stringify(p)}`);let i=`- ${e.join(" ")}${t.length>0?` \u2014 ${t.join("; ")}`:""}`,c=n.description?`
  ${K(n.description.replace(/\n+/g," "),240)}`:"",a=v(n.provenance,{}),l=a.declaredIn?`
  _Declared in \`${a.declaredIn}\`${a.includeChain&&a.includeChain.length>1?` via ${a.includeChain.map(d=>`\`${d}\``).join(" \u2192 ")}`:""}._`:"";return`${i}${c}${l}`}function bn(n,e){return n===!0?"":typeof n=="string"?`"${n.replace(/"/g,'\\"')}"`:typeof n=="number"?`<${n}>`:Array.isArray(n)?e==="string-array"?n.map(t=>`"${String(t)}"`).join(", "):e==="uint8-array"?`[${n.map(t=>Number(t).toString(16).padStart(2,"0")).join(" ")}]`:`<${n.map(String).join(" ")}>`:null}function yn(n){let e=v(n.const_value,void 0),t=v(n.default_value,void 0),r=v(n.enum_values,[]),o=Array.isArray(r)?r:[],s=e??t??o[0],i=bn(s,n.type);if(i!==null)return i===""?`        ${n.name};`:`        ${n.name} = ${i};`;switch(n.type){case"boolean":return`        ${n.name};`;case"string":case"string-array":return`        ${n.name} = "replace-me";`;case"int":case"array":return`        ${n.name} = <0>;`;case"uint8-array":return`        ${n.name} = [00];`;case"phandle":case"phandles":return`        ${n.name} = <&replace_me>;`;case"phandle-array":return`        ${n.name} = <&replace_me 0>;`;case"path":return`        ${n.name} = &replace_me;`;default:return`        /* Required: ${n.name} (${n.type??"binding-specific value"}). */`}}function ot(n){let e=n.trim();return/^0[xX][0-9a-fA-F]+$/.test(e)?Number.parseInt(e.slice(2),16):/^[0-9]+$/.test(e)?Number.parseInt(e,10):null}function _n(n,e,t,r){let o=a=>`0x${a.toString(16).padStart(2,"0")}`,s=[];try{s=n.all(`SELECT d.compatible, d.driver_file, d.register, d.register_name, v.name
         FROM driver_identity_value v JOIN driver_identity d ON d.id = v.identity_id
        WHERE v.value = ?${t===null?"":" AND d.register = ?"}
        ORDER BY d.compatible
        LIMIT ?`,...t===null?[e,r]:[e,t,r])}catch{}let i=t===null?"":` at register ${o(t)}`;if(s.length===0)return L("drivers accepting that identity value",`${o(e)}${i}`,"This index records an identity contract only for drivers whose check it could read, so a miss is not proof that no driver accepts the value. Search by part name or peripheral type instead, and read the candidate driver with get_source to see what it compares against.");let c=s.map(a=>({compatible:String(a.compatible),constant:String(a.name),register:a.register===null?null:Number(a.register),registerName:String(a.register_name??""),driverFile:String(a.driver_file)}));return I(`${c.length} driver(s) accept ${o(e)}${i}.

`+c.map(a=>`### \`${a.compatible}\`
accepts it as \`${a.constant}\`${a.register===null?"":`, read from ${o(a.register)}`}${a.registerName?` (\`${a.registerName}\`)`:""}
_driver: ${a.driverFile}_`).join(`

`)+`

An identity value is not unique across vendors: several unrelated parts answer 0x68. Confirm the candidate with get_binding, and check that the part is on the bus and address the driver expects.`,{identityValue:e,register:t,count:c.length,results:c})}var at=n=>({name:"search_bindings",title:"Search devicetree bindings",description:'Search Zephyr devicetree bindings by compatible string, hardware description, or property name. Use this to find the right compatible for a peripheral or sensor ("bosch bme280", "stm32 spi", "gpio led"), or to find which bindings accept a given property. Matches part of a compatible, so "digi-temp" finds st,stm32-digi-temp when the vendor prefix is not known. Pass "identity_value" to search the other direction: given the value an unknown part returns from its WHO_AM_I or chip-ID register, it reports which compatibles have a driver that accepts it. That is the question at a bench, and no other tool can answer it. Returns compatible strings; pass one to get_binding for the full property set.',inputSchema:{type:"object",properties:{query:{type:"string",description:"Compatible fragment, hardware description, or property name."},identity_value:{type:"string",description:`A value read from an unknown part's identity register, e.g. "0x19". Reports which compatibles have a driver that accepts it.`},identity_register:{type:"string",description:'Restrict the identity search to drivers that read this register, e.g. "0x75". Use when the register is known, since the same value is accepted by unrelated parts.'},vendor:{type:"string",description:'Restrict to a devicetree vendor prefix, e.g. "st", "espressif", "nordic".'},bus:{type:"string",description:'Restrict to a bus, e.g. "spi", "i2c".'},limit:F(15)},anyOf:[{required:["query"]},{required:["identity_value"]}],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=k(e,"vendor"),r=k(e,"bus"),o=A(e.limit,15),s=k(e,"identity_value");if(s!==void 0){let g=ot(s);if(g===null)throw new E(`"identity_value" must be an integer, in hexadecimal or decimal: got "${s}".`);let $=k(e,"identity_register"),m=$===void 0?null:ot($);if($!==void 0&&m===null)throw new E(`"identity_register" must be an integer, in hexadecimal or decimal: got "${$}".`);return _n(n(),g,m,o)}let i=j(e,"query"),c=[],a=[];t&&(c.push("AND b.vendor = ?"),a.push(t)),r&&(c.push("AND (b.bus = ? OR b.on_bus = ?)"),a.push(r,r));let l=n(),d=l.search(`SELECT b.compatible, b.description, b.bus, b.on_bus, b.n_props, b.path
         FROM dt_fts f JOIN dt_binding b ON b.id = f.rowid
        WHERE dt_fts MATCH ? ${c.join(" ")}
        ORDER BY bm25(dt_fts, 10.0, 2.0, 1.0)
        LIMIT ?`,i,[...a,o],o),p=new Set(d.map(g=>String(g.compatible))),u=d.length<o&&i.length>=3?l.all(`SELECT b.compatible, b.description, b.bus, b.on_bus, b.n_props, b.path
               FROM dt_binding b
              WHERE b.compatible LIKE ? ESCAPE '\\' ${c.join(" ")}
              ORDER BY LENGTH(b.compatible), b.compatible
              LIMIT ?`,`%${i.trim().replace(/[%_\\]/g,"\\$&")}%`,...a,o).filter(g=>!p.has(String(g.compatible))):[];if(d.push(...u.slice(0,Math.max(0,o-d.length))),d.length===0)return L("devicetree bindings",i,'Try the vendor name alone, the peripheral type ("temperature sensor"), or search_docs for the subsystem. Remember that out-of-tree drivers are not in this index.');let h=new Map;try{for(let g of l.all(`SELECT compatible,
                COUNT(DISTINCT NULLIF(board, '')) AS boards,
                COUNT(DISTINCT file) AS files
           FROM dt_instance
          WHERE compatible IN (${d.map(()=>"?").join(",")})
          GROUP BY compatible`,...d.map($=>String($.compatible)))){let $=l.all("SELECT DISTINCT file FROM dt_instance WHERE compatible = ? AND board = '' LIMIT 200",String(g.compatible)),m=[...new Set($.map(b=>String(b.file)).map(b=>b.lastIndexOf("/")<0?"":b.slice(0,b.lastIndexOf("/"))).filter(Boolean))].sort();h.set(String(g.compatible),{boards:Number(g.boards??0),files:Number(g.files??0),dirs:m})}}catch{}let f=d.map(g=>{let $=String(g.compatible),m=h.get($);return{compatible:$,description:K(String(g.description??"").replace(/\n+/g," "),200),bus:g.bus??null,onBus:g.on_bus??null,propertyCount:Number(g.n_props),path:String(g.path),boardsUsing:m?.boards??0,filesUsing:m?.files??0,socDirectories:m?.dirs??[]}}),w=f.map(g=>{let $=[g.bus?`bus: ${g.bus}`:"",g.onBus?`on-bus: ${g.onBus}`:""].filter(Boolean).join(", "),m=g.socDirectories.length>0?`named under ${g.socDirectories.slice(0,3).join(", ")}${g.socDirectories.length>3?` +${g.socDirectories.length-3} more`:""}${g.boardsUsing>0?`, and in ${g.boardsUsing} board file(s)`:""}`:g.boardsUsing>0?`named in ${g.boardsUsing} board file(s)`:g.filesUsing>0?`named in ${g.filesUsing} devicetree file(s)`:"not named in any devicetree here";return`### \`${g.compatible}\`${$?`  (${$})`:""}
${g.description||"_no description_"}
_${g.propertyCount} properties \xB7 ${m} \u2014 ${g.path}_`}).join(`

`);return I(`Found ${f.length} binding(s) for "${i}".

${w}`,{query:i,count:f.length,results:f})}});function be(n,e){return n.all(`SELECT id, compatible, path, description, bus, on_bus, cells, includes
       FROM dt_binding WHERE compatible = ? ORDER BY COALESCE(on_bus, ''), path`,e)}function ct(n,e){if(n.length<2)return;let t=n.map(r=>r.on_bus?String(r.on_bus):"no bus");return`**This compatible has ${n.length} bindings, one per bus: ${t.map(r=>`\`${r}\``).join(", ")}.** They do not require the same properties. The answer above is the \`${t[0]}\` one; pass \`on_bus\` to get_binding to select another, e.g. \`{ "compatible": "${e}", "on_bus": "${t[t.length-1]}" }\`.`}function lt(n,e){let t=n.search(`SELECT b.compatible FROM dt_fts f JOIN dt_binding b ON b.id = f.rowid
      WHERE dt_fts MATCH ? ORDER BY bm25(dt_fts, 10.0, 2.0, 1.0) LIMIT 8`,e.replace(/[,_-]/g," "),[],8);return U("Devicetree compatible",e,n.meta.zephyr_version??"unknown",t.map(r=>String(r.compatible)),"Bindings from external modules and compatible declarations not represented by this catalogue may not be covered.")}function dt(n,e){let t;try{t=n.all("SELECT file, board, node FROM dt_instance WHERE compatible = ? ORDER BY file, node",e)}catch{return{structured:{}}}if(t.length===0)return{text:"**No devicetree in this tree uses this compatible.** There is no upstream example of it in use and no indication of which silicon it targets. Out-of-tree boards may still use it, so this is not proof it is wrong. Read the driver before adopting it.",structured:{usedIn:{files:0,socDirectories:[],boards:[]}}};let r=new Map;for(let a of t){let l=String(a.board);if(!l)continue;let d=String(a.node??""),p=r.get(l);p?p.add(d):r.set(l,new Set([d]))}let o=[...r.keys()].sort(),s=a=>{let l=[...r.get(a)??[]].filter(Boolean).sort();return l.length===0?`\`${a}\``:`\`${a}\` as ${l.map(d=>`\`${d}\``).join(", ")}`},i=[...new Set(t.filter(a=>!a.board).map(a=>{let l=String(a.file),d=l.lastIndexOf("/");return d<0?"":l.slice(0,d)}).filter(Boolean))].sort(),c=[];return i.length>0&&c.push(`**Named in SoC and shared devicetree under**: ${i.slice(0,8).map(a=>`\`${a}\``).join(", ")}${i.length>8?`, and ${i.length-8} more`:""}. These directories indicate the silicon the driver targets. If your part is not among them, treat the driver as unverified on your part.`),o.length>0&&c.push(`**Declared directly in ${o.length} board file${o.length===1?"":"s"}**: ${o.slice(0,8).map(s).join(", ")}${o.length>8?`, and ${o.length-8} more`:""}. The node name is the part the board actually carries, which is often a different part number from the compatible.`),c.push("_A board normally enables an SoC peripheral by referencing its label, not by repeating the compatible, so a low board count does not mean the driver is unused. Use the devicetree directories instead. Where a driver is used indicates what it was written for; it does not guarantee compatibility. If your part is not covered, read the driver with get_source, note the registers it uses, and check they exist in your SoC vendor header, which get_source also reads from the HAL module._"),{text:c.join(`

`),structured:{usedIn:{files:t.length,socDirectories:i,boards:o}}}}function ut(n,e){let t;try{t=n.all(`SELECT d.driver_file, d.register_name, d.register, v.value, v.name
         FROM driver_identity d JOIN driver_identity_value v ON v.identity_id = d.id
        WHERE d.compatible = ?
        ORDER BY d.driver_file, v.ord`,e)}catch{return{structured:{}}}if(t.length===0)return{structured:{identity:null}};let r=a=>`0x${a.toString(16).padStart(2,"0")}`,o=String(t[0].driver_file),s=String(t[0].register_name??""),i=t[0].register===null?null:Number(t[0].register),c=t.map(a=>({value:Number(a.value),name:String(a.name)}));return{text:"**Driver identity check.** "+(i===null?"This driver reads an identity register and accepts ":`This driver reads ${r(i)}${s?` (\`${s}\`)`:""} and accepts `)+`${c.map(a=>`${r(a.value)} \`${a.name}\``).join(", ")}. A part that returns anything else fails initialisation, so read the register off the part before binding this driver. Binding one whose check the part fails produces a device that initialises, or half-initialises, and returns numbers that are not readings. Source: \`${o}\`.`,structured:{identity:{driverFile:o,register:i,registerName:s,accepts:c}}}}function wn(n,e){let t=e.replace(/^["']|["']$/g,""),r=be(n,t),o=r[0];if(!o)return{key:t,text:`### \`${t}\`

${lt(n,t)}`,structured:{compatible:t,found:!1}};let i=n.all("SELECT required, child_level, name FROM dt_property_v WHERE binding_id = ?",Number(o.id)).filter(u=>Number(u.child_level)===0&&!st.has(String(u.name))),c=i.filter(u=>Number(u.required)===1).map(u=>String(u.name)),a=i.length-c.length,l=v(o.includes,[]),d=dt(n,t),p=ut(n,t);return{key:t,text:O([`### \`${t}\``,o.description?String(o.description).trim():void 0,o.bus?`**Is a bus controller for:** \`${String(o.bus)}\``:void 0,o.on_bus?`**Must be a child of a** \`${String(o.on_bus)}\` **bus node**`:void 0,c.length>0?`**Required properties** (${c.length}): ${c.map(u=>`\`${u}\``).join(", ")}`:"**Required properties**: none beyond the universal ones.",p.text,d.text,ct(r,t),`_${a} optional propert${a===1?"y":"ies"} and the full flattened set are behind a single-compatible get_binding call. Binding file: \`${String(o.path)}\`._`,l.length>0?`_Inherits from: ${l.map(u=>`\`${u}\``).join(", ")}_`:void 0]),structured:{compatible:t,found:!0,path:o.path,description:o.description??"",bus:o.bus??null,onBus:o.on_bus??null,required:c,optionalCount:a,includes:l,...d.structured,...p.structured}}}var pt=n=>({name:"get_binding",title:"Get devicetree bindings",description:`Get the complete, flattened property set a devicetree compatible accepts, with each property's type, whether it is required, its allowed values, and which binding file it is inherited from. Also reports which SoC and board devicetree files name the compatible, which indicates what silicon the driver was written for. Vendors reuse peripheral names across incompatible register layouts, and a node using the wrong one compiles and does nothing. Use this before writing or editing any devicetree node or overlay. Zephyr bindings inherit most of their properties through include: chains, so the binding file itself usually lists few of them: st,stm32-spi declares no properties in its own file and accepts about forty. Pass "compatibles" to check several at once, which returns each one's bus, required properties and include chain; the full property set requires a single-compatible call. A device reachable over more than one bus has one binding per bus, and they require different properties: a SPI node needs spi-max-frequency and an I2C node does not. Pass "on_bus" for the bus the node sits on.`,inputSchema:{type:"object",properties:{compatible:{type:"string",description:'The compatible string, e.g. "st,stm32-spi" or "bosch,bme280".'},compatibles:ee('Several compatibles in one call, e.g. ["st,lsm6dsl", "st,hts221"]. Returns a compact summary of each instead of the full flattened property table.'),on_bus:{type:"string",description:'Select the binding for the bus this node sits on, e.g. "spi" or "i2c". Only matters for a device with a binding per bus, where the required properties differ.'},include_common:{type:"boolean",default:!1,description:"Include universally available properties (status, compatible, power-domains, ...). Off by default to keep the answer focused on what is specific to this device."}},additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let{values:t,batched:r}=J(e,"compatible","compatibles"),o=e.include_common===!0,s=n();if(r)return X(t.map(_=>wn(s,_)),Q);let i=t[0].replace(/^["']|["']$/g,""),c=k(e,"on_bus"),a=be(s,i);if(a.length===0)throw new E(lt(s,i));let l=c?a.find(_=>String(_.on_bus??"")===c):a[0];if(!l){let _=a.map(S=>S.on_bus?String(S.on_bus):"no bus");throw new E(`"${i}" has no binding for a "${c}" bus. It is declared for: ${_.map(S=>`"${S}"`).join(", ")}.`)}let d=Number(l.id),p=s.all(`SELECT name, type, required, description, default_value, enum_values, const_value,
              deprecated, specifier_space, inherited_from, child_level, child_path,
              provenance, constraints
         FROM dt_property_v WHERE binding_id = ? ORDER BY child_level, required DESC, name`,d),u=o?p:p.filter(_=>!st.has(_.name)),h=u.filter(_=>_.child_level===0),f=h.filter(_=>_.required),w=h.filter(_=>!_.required),g=u.filter(_=>_.child_level>0),$=v(l.cells,{}),m=v(l.includes,[]),b=ut(s,i),y=dt(s,i),T=["```dts","example_node: node@0 {",`        compatible = "${i}";`,...f.filter(_=>_.name!=="compatible").map(yn),'        status = "okay";',"};","```"].join(`
`),x=O([`# \`${i}\``,l.description?String(l.description):void 0,l.bus||l.on_bus?[l.bus?`**Is a bus controller for:** \`${String(l.bus)}\``:"",l.on_bus?`**Must be a child of a** \`${String(l.on_bus)}\` **bus node**`:""].filter(Boolean).join(`
`):void 0,c?void 0:ct(a,i),b.text,y.text,Object.keys($).length>0?`**Specifier cells**
${Object.entries($).map(([_,S])=>`- \`${_}\`: ${S.join(", ")}`).join(`
`)}`:void 0,f.length>0?`## Required properties (${f.length})
${f.map(Ee).join(`
`)}`:`## Required properties
_None beyond the universal ones._`,w.length>0?`## Optional properties (${w.length})
${w.map(Ee).join(`
`)}`:void 0,g.length>0?`## Child node properties (${g.length})
This binding defines child nodes with their own properties:
${g.map(Ee).join(`
`)}`:void 0,`## Skeleton
${T}`,m.length>0?`_Inherits from: ${m.map(_=>`\`${_}\``).join(", ")}_`:void 0,`_Binding file: \`${String(l.path)}\`${o?"":`. ${p.length-u.length} universal properties hidden; pass include_common: true to see them_`}_`]);return I(x,{compatible:i,found:!0,path:l.path,description:l.description??"",bus:l.bus??null,onBus:l.on_bus??null,busVariants:a.map(_=>({onBus:_.on_bus??null,path:String(_.path)})),...b.structured,...y.structured,cells:$,includes:m,required:f.map(_=>({name:_.name,type:_.type,description:_.description,provenance:v(_.provenance,{}),constraints:v(_.constraints,{})})),optional:w.map(_=>({name:_.name,type:_.type,description:_.description,provenance:v(_.provenance,{}),constraints:v(_.constraints,{})})),childProperties:g.map(_=>({name:_.name,type:_.type,level:_.child_level,childPath:_.child_path,provenance:v(_.provenance,{}),constraints:v(_.constraints,{})}))})}});var $n=200;function Sn(n){let e=n.replace(/\r\n?/g,`
`).split(`
`),t=[];for(let r=0;r<e.length;r++){let o=r+1,s=e[r];for(;/\\\s*$/.test(s)&&r+1<e.length;)s=s.replace(/\\\s*$/,"")+e[++r].trimStart();t.push({line:o,text:s})}return t}function vn(n,e="CONFIG_"){let t=[],r=[],o=[],s=/^\s*(#\s*)?(SB_CONFIG_|CONFIG_)([A-Za-z0-9_]+)\s*(.*)$/;for(let i of Sn(n)){let c=i.text;if(/^\s*$/.test(c))continue;let a=c.match(s);if(a&&a[2]!==e){o.push({name:a[3],value:"",line:i.line,raw:c.trim()});continue}if(a?.[1]){/^is\s+not\s+set$/.test((a[4]??"").trim())?t.push({name:a[3],value:"n",line:i.line,raw:c.trim()}):/\bis\s+not\b/.test(c)&&r.push({name:"",value:"",line:i.line,raw:c.trim()});continue}if(!/^\s*#/.test(c)&&a){let l=(a[4]??"").trim(),d=l.startsWith("=")?l.slice(1).trim():"";l.startsWith("=")&&d!==""?t.push({name:a[3],value:d,line:i.line,raw:c.trim()}):r.push({name:"",value:"",line:i.line,raw:c.trim()})}}return{assignments:t,malformed:r,foreign:o}}function En(n,e){return n==="bool"||n==="tristate"?(n==="bool"?/^[yn]/:/^[ymn]/).test(e)?null:`is ${n} but is set to "${e}" (expected ${n==="bool"?"y or n":"y, m, or n"})`:n==="int"&&!/^-?[0-9]+$/.test(e)?`is int but is set to "${e}"`:n==="hex"&&!/^(?:0x)?[0-9a-fA-F]+$/.test(e)?`is hex but is set to "${e}"`:n==="string"&&!/^"(?:[^"\\]|\\.)*"$/.test(e)?"is string but is not a quoted string":null}function xn(n){return n.replace(/\/\*[\s\S]*?\*\//g,e=>e.replace(/[^\n]/g," ")).replace(/\/\/[^\n]*/g,e=>" ".repeat(e.length))}function Rn(n){let e=xn(n.replace(/\r\n?/g,`
`)),t=[];for(let r of e.matchAll(/(?:^|[\s;{}])compatible\s*=\s*([^;{}]*);/g)){let o=[...r[1].matchAll(/"([^"]*)"/g)].map(s=>s[1]).filter(Boolean);o.length!==0&&t.push({values:o,line:e.slice(0,r.index).split(`
`).length})}return t}function kn(n,e){let t=n.indexOf(","),r=t<0?null:n.slice(0,t),o=null,s=1/0;for(let i of e){if(Math.abs(i.length-n.length)>2||r!==null&&!i.startsWith(`${r},`)||r===null&&i.includes(","))continue;let c=le(n,i);c<s&&(s=c,o=i)}return!o||s===0||s>2?null:1-s/Math.max(n.length,o.length)>=.85?{name:o,edits:s}:null}function Tn(n){return n&&/(^|\/)sysbuild\.conf$/.test(n)?"SB_CONFIG_":"CONFIG_"}function In(n,e){if(n){if(/\.(dts|dtsi|overlay)$/.test(n))return"devicetree";if(/(\.conf|_defconfig)$/.test(n))return"kconfig"}if(/(^|[\s;{}])compatible\s*=/.test(e))return"devicetree";if(/^\s*(?:#\s*)?(?:SB_)?CONFIG_[A-Za-z0-9_]+\s*[=\s]/m.test(e))return"kconfig";throw new E('Could not tell whether this is a Kconfig fragment or devicetree source. Pass "kind" as "kconfig" or "devicetree", or pass "path" so the extension can decide.')}function On(n,e,t,r="CONFIG_"){let o=r==="SB_CONFIG_"?"sysbuild":"zephyr",s=vn(e,r),i=[];for(let u of s.foreign){let h=r==="SB_CONFIG_"?"CONFIG_":"SB_CONFIG_",f=u.name&&n.get("SELECT 1 FROM kconfig WHERE name = ? AND scope = ?",u.name,o)?`; \`${r}${u.name}\` exists and may be what was meant`:"";i.push({line:u.line,subject:u.raw,status:"problem",problem:`is a ${h} line in a ${r==="SB_CONFIG_"?"sysbuild.conf":"prj.conf"}-style file, where only ${r} settings apply. The build ignores it silently${f}`,note:""})}for(let u of s.malformed)i.push({line:u.line,subject:u.raw,status:"problem",problem:`malformed Kconfig assignment: ${u.raw}`,note:""});let c=new Set(n.all("SELECT glue_dir FROM west_module WHERE glue_dir <> '' AND kconfig_ingested = 0").map(u=>String(u.glue_dir))),a=n.db.prepare(`
    SELECT k.name AS name, k.type AS type, k.prompt AS prompt, k.has_prompt AS has_prompt,
           (SELECT COUNT(*) FROM kconfig_definition d WHERE d.symbol_id = k.id) AS defs,
           (SELECT group_concat(DISTINCT d.file) FROM kconfig_definition d
             WHERE d.symbol_id = k.id) AS files,
           (SELECT COUNT(*) FROM kconfig_definition d WHERE d.symbol_id = k.id
             AND d.file LIKE '%Kconfig.defconfig%') AS defconfig_defs
      FROM kconfig k WHERE k.name = ? AND k.scope = ?`),l=u=>String(u??"").split(",").some(h=>{let f=/^modules\/([^/]+)\//.exec(h);return f?c.has(f[1]):!1}),d=(u,h)=>{if(u.type===null||Number(u.defs)===0)return"no declaration for this symbol was indexed, so it has no type or prompt here";let f=String(u.files??"").split(",").map(w=>/^modules\/([^/]+)\//.exec(w)?.[1]??"").find(w=>w&&h.has(w));return f?`the only declarations are in \`modules/${f}\`, whose module Kconfig this index did not read, so a prompt the module itself declares is not visible here`:"every indexed declaration is a `Kconfig.defconfig`, which sets a value rather than declaring the symbol, so its prompt is declared somewhere this index did not read"},p=u=>u.type!==null&&Number(u.defs)>0&&!l(u.files)&&Number(u.defconfig_defs)<Number(u.defs);for(let u of s.assignments){let h=a.get(u.name,o);if(!h){let m=n.all("SELECT name FROM kconfig WHERE name LIKE ? ESCAPE '\\' AND scope = ? ORDER BY LENGTH(name) LIMIT 4",`${u.name.slice(0,Math.max(4,u.name.length-4)).replace(/[%_]/g,"\\$&")}%`,o).map(b=>`${r}${String(b.name)}`);i.push({line:u.line,subject:u.raw,status:"not-judged",problem:null,note:"not in the indexed catalogue. Generated, application-local, board-derived and external-module symbols live outside it, so this is not proof the symbol is wrong"+(m.length>0?`. Similar indexed names: ${m.join(", ")}`:"")});continue}let f=String(h.type??""),w=h.prompt?String(h.prompt):"";if(!t&&Number(h.has_prompt)===0&&p(h)){let m=n.all("SELECT DISTINCT from_sym FROM kconfig_edge WHERE to_sym = ? AND kind = ? AND scope = ? ORDER BY from_sym LIMIT 4",u.name,"select",o).map(b=>`${r}${String(b.from_sym)}`);i.push({line:u.line,subject:u.raw,status:"problem",problem:`CONFIG_${u.name} has no prompt and cannot be assigned from an application configuration. Enable the symbol that selects it instead`+(m.length>0?`: ${m.join(", ")}`:"."),note:""});continue}let g=En(f,u.value);if(g){i.push({line:u.line,subject:u.raw,status:"problem",problem:`CONFIG_${u.name} ${g}.`,note:""});continue}let $=Number(h.has_prompt)===0&&!p(h);i.push({line:u.line,subject:u.raw,status:$?"not-judged":"ok",problem:null,note:$?`not judged \u2014 ${d(h,c)}. The type is \`${f||"unknown"}\`, but whether this line can be assigned here was not established`:`ok, \`${f||"unknown"}\`${w?` "${w}"`:""}`})}return i.sort((u,h)=>u.line-h.line)}function Nn(n,e){let t=Rn(e),r=n.db.prepare("SELECT 1 FROM dt_binding WHERE compatible = ?"),o=null,s=[];for(let i of t){let c=i.values.some(a=>r.get(a)!==void 0);for(let a of i.values){if(r.get(a)!==void 0){let d=be(n,a),p=d.map(u=>{let h=Number(n.get("SELECT COUNT(*) AS c FROM dt_property_v WHERE binding_id = ? AND required = 1",Number(u.id))?.c??0),f=u.on_bus?String(u.on_bus):null;return`${f?`on \`${f}\`: `:""}${h} required propert${h===1?"y":"ies"}`}).join("; ");s.push({line:i.line,subject:a,status:"ok",problem:null,note:`indexed \u2014 ${p}`+(d.length>1?". The bindings differ by bus; use get_binding with `on_bus` for the one this node sits on":"")});continue}o??=n.all("SELECT compatible FROM dt_binding").map(d=>String(d.compatible));let l=c?null:kn(a,o);s.push({line:i.line,subject:a,status:l?"problem":c?"ok":"not-judged",problem:l?`"${a}" is not a known compatible. "${l.name}" is, and differs by ${l.edits} character${l.edits===1?"":"s"}. A node whose compatible resolves to no binding is dropped, along with every property on it, without a warning.`:null,note:c?"not indexed itself, but another compatible on this node is, so the node still binds":"not in the indexed catalogue and not close to anything in it. An application may declare its own binding through `dts/bindings`, `DTS_ROOT`, or a module"})}}return s.sort((i,c)=>i.line-c.line)}var ht=n=>({name:"check_config",title:"Check a configuration file against the index",description:"Check a whole prj.conf, board .conf, defconfig, sysbuild.conf, .overlay, .dts or .dtsi against the indexed Zephyr version and get a verdict for every line, in one call. Use it before writing a configuration file and again after. Every line comes back in one of three populations: confirmed against the catalogue, carrying a problem, or not judged \u2014 the last meaning the catalogue could not settle it and checked nothing, with the reason given. It reports each symbol's type, prompt and whether it can be assigned; each compatible's bus and required-property count; and four errors: a malformed assignment, a promptless symbol assigned from an application configuration, a value that contradicts the declared type, and a compatible that misspells an indexed one. A grep only tells you whether a name appears somewhere. A symbol or compatible that is absent from the index is reported as outside the catalogue, not as wrong.",inputSchema:{type:"object",properties:{text:{type:"string",minLength:1,maxLength:2e5,description:"The complete file contents to check."},path:{type:"string",description:'Optional file path, e.g. "prj.conf" or "boards/nucleo_h743zi.overlay". Decides which catalogue applies, and suppresses the promptless finding for a _defconfig, where assigning a promptless symbol is legitimate.'},kind:{type:"string",enum:["kconfig","devicetree"],description:"Override the file kind when the path does not state it."},namespace:{type:"string",enum:["CONFIG_","SB_CONFIG_"],description:"Which Kconfig namespace the file is written in. Inferred from the path, where only sysbuild.conf is SB_CONFIG_. Pass this only for an unnamed sysbuild fragment."}},required:["text"],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=j(e,"text"),r=k(e,"path"),o=k(e,"kind")??In(r,t),s=n(),i=s.meta.zephyr_version??"unknown",c=k(e,"namespace")??Tn(r),a=o==="kconfig"?On(s,t,!!r?.endsWith("_defconfig"),c):Nn(s,t),l=a.filter(w=>w.problem!==null),d=a.filter(w=>w.status==="ok").length,p=a.filter(w=>w.status==="not-judged").length,u=o==="kconfig"?"Kconfig assignment":"compatible declaration",h=a.slice(0,$n),f=O([`# ${r?`\`${r}\``:`${o} fragment`}`,`Checked ${a.length} ${u}${a.length===1?"":"s"} against the indexed Zephyr ${i} catalogue: ${d} confirmed, ${p} not judged, ${l.length} with a problem.`+(p>0?" A line that was not judged was checked against nothing; the reason is on the line itself. It is neither an error nor a confirmation.":""),l.length>0?`## Problems (${l.length})
`+l.map(w=>`- line ${w.line}: ${w.problem}`).join(`
`):`## Problems
_None. Every line above is either confirmed against the catalogue or outside its scope; see the per-line verdicts._`,h.length>0?`## Per line
`+h.map(w=>`- line ${w.line} \`${w.subject}\` \u2014 ${w.problem??w.note}`).join(`
`):void 0,a.length>h.length?`_${a.length-h.length} further line verdict(s) are not shown; every problem is listed above regardless._`:void 0,o==="devicetree"?"_Property names are not checked. Bindings inherit most of their properties through `include:` chains, so get_binding is the only reliable source for them._":void 0]);return I(f,{kind:o,path:r??null,zephyrVersion:i,checked:a.length,confirmedCount:d,notJudgedCount:p,problemCount:l.length,verdicts:a})}});import{existsSync as ke,readFileSync as Ln,readdirSync as Fn}from"node:fs";import{homedir as Hn}from"node:os";import{join as gt}from"node:path";import{spawnSync as bt}from"node:child_process";import{existsSync as xe,readFileSync as Cn,realpathSync as An}from"node:fs";import{delimiter as jn,join as Dn,resolve as Mn}from"node:path";function mt(n,e){if(n.includes("/")||n.includes("\\"))return xe(n)?Mn(n):void 0;for(let t of(e??"").split(jn).filter(Boolean)){let r=Dn(t,n);if(xe(r))return r}}function Re(n){let e=mt("west",n.PATH);if(e)try{let r=(Cn(An(e),"utf8").split(/\r?\n/,1)[0]??"").match(/^#!\s*(\S+)(?:\s+(.+))?$/);return r?r[1]?.endsWith("/env")&&r[2]?mt(r[2].trim().split(/\s+/,1)[0],n.PATH):r[1]&&xe(r[1])?r[1]:void 0:void 0}catch{return}}function ft(n){return[n.PYTHON_EXECUTABLE,Re(n),"python3","python"].filter((e,t,r)=>!!e&&r.indexOf(e)===t)}var yt=5e3,Pn=`
import json, sys
try:
    from importlib.metadata import distribution
except Exception:
    print(json.dumps({"error": "importlib.metadata unavailable"})); raise SystemExit(0)
try:
    from packaging.markers import Marker
except Exception:
    Marker = None
requirements = json.loads(sys.argv[1])
missing, unevaluated = [], []
for entry in requirements:
    marker = entry.get("marker")
    if marker:
        if Marker is None:
            unevaluated.append(entry["name"]); continue
        try:
            if not Marker(marker).evaluate():
                continue
        except Exception:
            unevaluated.append(entry["name"]); continue
    try:
        distribution(entry["name"])
    except Exception:
        missing.append(entry["name"])
print(json.dumps({
    "version": ".".join(str(p) for p in sys.version_info[:3]),
    "executable": sys.executable,
    "missing": missing,
    "unevaluated": unevaluated,
}))
`;function Bn(n,e,t){let r=bt(n,["-c",Pn,JSON.stringify(e)],{encoding:"utf8",timeout:yt,env:{...process.env,PYTHONDONTWRITEBYTECODE:"1"}});if(r.status!==0||!r.stdout.trim())return{path:n,resolved:null,version:null,role:t,missing:[],unevaluated:[],error:r.error?r.error.message:r.stderr.trim().split(`
`).slice(-2).join(" ")};let o=JSON.parse(r.stdout);return{path:n,resolved:o.executable??null,version:o.version??null,role:t,missing:o.missing??[],unevaluated:o.unevaluated??[],...o.error?{error:o.error}:{}}}function Wn(n,e){let t=bt(n,e,{encoding:"utf8",timeout:yt});return t.status!==0?null:`${t.stdout}${t.stderr}`.trim().split(`
`)[0]??null}function qn(){let n=process.env.ZEPHYR_SDK_INSTALL_DIR;if(n&&ke(n))return{found:!0,detail:`ZEPHYR_SDK_INSTALL_DIR=${n}`};let e=gt(Hn(),".cmake","packages","Zephyr-sdk");if(ke(e))try{let t=Fn(e).map(r=>Ln(gt(e,r),"utf8").trim()).filter(r=>ke(r));if(t.length>0)return{found:!0,detail:`registered with CMake: ${t.join(", ")}`}}catch{}return{found:!1,detail:"no ZEPHYR_SDK_INSTALL_DIR and nothing in the CMake registry"}}var _t=n=>({name:"check_environment",title:"Check the build environment",description:"Check whether this machine can build the indexed Zephyr version, and give the command that fixes each gap. Use before the first `west build` in a session, and whenever a build fails with a missing Python module, a missing toolchain, or a CMake error naming jsonschema, pykwalify, or elftools. It lists every Python interpreter it can find and which required packages each one has. The interpreter that satisfies the indexer is often not the one CMake selects for a build; a west installed in its own environment is the common cause. This tool only reports. It does not install anything.",inputSchema:{type:"object",properties:{board:{type:"string",description:"Optional board or qualified target. Adds the runners this board needs on the host."}},additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!1,openWorldHint:!0},handler:e=>{let t=n(),r=k(e,"board"),o=v(t.meta.python_requirements,[]),s=t.meta.zephyr_version??"unknown",i=Re(process.env),c=new Map;for(let b of ft(process.env)){let y=b===process.env.PYTHON_EXECUTABLE?"PYTHON_EXECUTABLE, which the indexer prefers":b===i?"west's own interpreter, used by the indexer and not by a build":b==="python3"?"python3 on PATH, which is what CMake selects for a build":"fallback on PATH";c.set(b,y)}let a=[...c.entries()].map(([b,y])=>Bn(b,o,y)).filter(b=>b.version!==null||!/ENOENT/.test(b.error??"")),l=a.find(b=>b.path==="python3"&&b.version),d=Wn("west",["--version"]),p=qn(),u=t.get("SELECT 1 AS present FROM west_command WHERE name = ?","sdk")!==void 0,h=[],f=o.length>0;l?l.missing.length>0&&h.push(`The interpreter a build will use (\`${l.resolved??"python3"}\`) is missing ${l.missing.length} of the packages Zephyr ${s} requires: ${l.missing.map(b=>`\`${b}\``).join(", ")}.

Install them into that interpreter's environment:

\`\`\`bash
# with uv
uv pip install --python "$(command -v python3)" -r <zephyr>/scripts/requirements-base.txt
# or with pip
python3 -m pip install -r <zephyr>/scripts/requirements-base.txt
\`\`\``):h.push("No `python3` on PATH. CMake resolves Python from PATH, so a build cannot start. Install Python 3.12 or newer and make sure `python3` resolves to it."),d||h.push("No `west` on PATH. Install it \u2014 `uv tool install west` keeps it isolated, `python3 -m pip install west` puts it in the same environment as the build."),p.found||h.push(`No Zephyr SDK found (${p.detail}). `+(u?"This Zephyr ships `west sdk`, so `west sdk install` fetches a matching toolchain and registers it. Add `-t <toolchain>` to install only what your targets need.":"This Zephyr version has no `west sdk` command; install the SDK from the zephyrproject-rtos/sdk-ng releases and run its `setup.sh`."));let w=a.filter(b=>b.version&&b.missing.length===0),g=l&&l.missing.length>0&&w.length>0?`\`${w[0].path}\` has everything and \`python3\` does not. The index was built with the former, which is why indexing succeeded while a build will not. Installing the requirements into the interpreter on PATH is what resolves this; a tool-scoped west cannot be repaired from inside, because it has no pip of its own.`:void 0,$=(()=>{if(!r)return;let b=r.split("/")[0].split("@")[0].trim(),y=t.all(`SELECT r.runner, r.available, r.flash_default, r.debug_default
           FROM board_runner r JOIN board b ON b.id = r.board_id
          WHERE b.name = ? AND r.available = 1 ORDER BY r.runner`,b);if(y.length===0)return R(`Host tools for \`${b}\``,["This board registers no runner in the indexed tree, so there is no host flashing tool to check."]);let T=y.map(x=>{let _=[Number(x.flash_default)===1?"`west flash`":"",Number(x.debug_default)===1?"`west debug`":""].filter(Boolean).join(" and ");return`\`${String(x.runner)}\`${_?` \u2014 used by ${_}`:""}`});return O([R(`Host tools for \`${b}\``,T),"Each runner wraps a separate host program, and they are not declared in a form this index can check \u2014 read `doc/develop/flash_debug/host-tools.rst` with get_doc for what each needs, and run `west flash --context` in a configured build for the authoritative list."])})(),m=O(["# Build environment",h.length===0?f?`Nothing blocking. A build of Zephyr ${s} should start on this machine.`:"No blocking problem found, but the package check did not run \u2014 see the note below, and treat this as unverified rather than clean.":h.length===1?`One thing will stop a build of Zephyr ${s}.`:`${h.length} things will stop a build of Zephyr ${s}.`,R("Python interpreters",a.map(b=>{if(!b.version)return`\`${b.path}\` \u2014 not usable${b.error?` (${b.error})`:""}`;let y=b.missing.length===0?"has every required package":`missing ${b.missing.map(x=>`\`${x}\``).join(", ")}`,T=b.unevaluated.length>0?` \xB7 not evaluated here: ${b.unevaluated.join(", ")}`:"";return`\`${b.resolved??b.path}\` \u2014 Python ${b.version} \xB7 ${b.role} \xB7 ${y}${T}`})),g,R("Tooling",[`west: ${d??"not on PATH"}`,`Node: ${process.version}`,`Zephyr SDK: ${p.found?p.detail:"not found"}`]),h.length>0?R("What to fix",h):void 0,$,f?void 0:"_This index records no Python requirements, so no interpreter was checked against one. That happens when the indexed tree has no `scripts/requirements-base.txt` \u2014 a partial checkout, usually. Nothing above says the environment is good; it says it was not checked._"]);return I(m,{zephyrVersion:s,ok:h.length===0&&f,packagesChecked:f,interpreters:a,buildInterpreter:l??null,west:d,node:process.version,sdk:p,shipsWestSdk:u,requirements:o.length,problems:h,...r?{board:r}:{}})}});var wt=n=>({name:"search_docs",title:"Search Zephyr documentation",description:'Search the Zephyr documentation for the version in use, returning the individual sections that match rather than whole pages. Covers the kernel, subsystems, driver models, connectivity stacks, build system, and every board page including pinouts and flashing instructions. Use it for concepts and procedures: "how do I use the sensor API", "power management states", "sysbuild". For a symbol, a compatible, or a board, use the dedicated tool instead; it is more precise.',inputSchema:{type:"object",properties:{query:{type:"string",description:"What you want to know about."},area:{type:"string",description:"Restrict to a documentation area: kernel, services, connectivity, hardware, build, develop, boards, contribute, security, safety, project, releases."},limit:F(8,25)},required:["query"],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=j(e,"query"),r=k(e,"area"),o=A(e.limit,8,25),s=[],i=[];r&&(s.push("AND d.area = ?"),i.push(r));let c=n().search(`SELECT d.path, d.url, d.title, d.area, c.heading, c.heading_path, c.anchor, c.body
         FROM doc_fts f
         JOIN doc_chunk c ON c.id = f.rowid
         JOIN doc d ON d.id = c.doc_id
        WHERE doc_fts MATCH ? ${s.join(" ")}
        ORDER BY bm25(doc_fts, 8.0, 4.0, 1.0)
        LIMIT ?`,t,[...i,o],o);if(c.length===0)return L("documentation sections",t,"Try fewer or more general words. For configuration options use search_kconfig, for devicetree use search_bindings, and for C functions use search_api.");let a=c.map(d=>({title:String(d.title),heading:String(d.heading??""),headingPath:String(d.heading_path??""),path:String(d.path),anchor:d.anchor??null,area:String(d.area),url:String(d.url)+(d.anchor?`#${String(d.anchor)}`:""),excerpt:K(String(d.body??""),700)})),l=a.map(d=>`### ${d.headingPath||d.title}
_${d.area} \xB7 \`${d.path}\`${d.anchor?` \xB7 anchor \`${d.anchor}\``:""}_

${d.excerpt}

${d.url}`).join(`

---

`);return I(`${a.length} section(s) matching "${t}".

${l}

Use get_doc with a path to read a full page.`,{query:t,count:a.length,results:a})}}),$t=n=>({name:"get_doc",title:"Read a documentation page",description:"Read a Zephyr documentation page in full, or a single section of it. Take the path from a search_docs result or from get_board (board pages carry the pinout and the flashing procedure for that hardware). Long pages are returned section by section unless a section is named.",inputSchema:{type:"object",properties:{path:{type:"string",description:'Source path of the page, e.g. "doc/services/sensor/index.rst" or "boards/st/nucleo_h743zi/doc/index.rst".'},section:{type:"string",description:"Optional: return only this section, matched against its heading or anchor label."},max_chars:{type:"integer",minimum:1e3,maximum:6e4,default:18e3,description:"Truncate the page at roughly this many characters."}},required:["path"],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=j(e,"path"),r=k(e,"section"),o=A(e.max_chars,18e3,6e4),s=n(),i=(s.meta.doc_base_url??"").replace(/\/?$/,"/"),c=t.replace(/#.*$/,""),a=i&&c.startsWith(i)?`doc/${c.slice(i.length).replace(/\.html?$/,".rst")}`:void 0,l=[t,c,a,c.replace(/\.html?$/,".rst"),`${c}.rst`,`doc/${c}`].filter($=>!!$),d;for(let $ of l)if(d=s.get("SELECT id, path, url, title, area FROM doc WHERE path = ? OR url = ?",$,$),d)break;if(!d){let $=c.replace(/[\\%_]/g,"\\$&"),m=s.all(`SELECT id, path, url, title, area FROM doc
          WHERE path LIKE ? ESCAPE '\\' ORDER BY LENGTH(path), path LIMIT 12`,`%/${$}`);if(m.length>1)throw new E(`Documentation path "${t}" is ambiguous. Use one exact path:
`+m.map(b=>`- ${String(b.path)}`).join(`
`));d=m[0]}if(!d)return ie("Documentation page",t,s.meta.zephyr_version??"unknown",[],"Module documentation and pages excluded by this index's coverage report may not be covered. Use search_docs to find an indexed path.");let p=s.all("SELECT heading, heading_path, anchor, body FROM doc_chunk WHERE doc_id = ? ORDER BY ord",Number(d.id)),u=r?p.filter($=>String($.heading??"").toLowerCase().includes(r.toLowerCase())||String($.anchor??"").toLowerCase()===r.toLowerCase()):p;if(u.length===0)throw new E(`Page \`${String(d.path)}\` has no section matching "${r}".

Sections:
`+p.map($=>`- ${String($.heading)}`).join(`
`));let h="",f=!1;for(let $ of u){let m=`## ${String($.heading)}

${String($.body??"")}

`;if(h.length+m.length>o){f=!0;break}h+=m}let w=`# ${String(d.title)}

_${String(d.area)} \xB7 \`${String(d.path)}\` \xB7 ${String(d.url)}_

`,g=f?`

_Truncated at ${o} characters. Remaining sections: ${u.slice(h.split("## ").length-1).map($=>String($.heading)).join(", ")}. Request one with the "section" argument._`:"";return I(w+h.trimEnd()+g,{path:d.path,url:d.url,title:d.title,found:!0,sections:u.map($=>String($.heading)),truncated:f})}});var zn=12;function St(n){let e=n.trim().toUpperCase();return e.startsWith("SB_CONFIG_")?{name:e.slice(10),scope:"sysbuild",prefix:"SB_CONFIG_"}:{name:e.replace(/^CONFIG_/,""),scope:"zephyr",prefix:"CONFIG_"}}var ne={zephyr:"CONFIG_",sysbuild:"SB_CONFIG_"};function vt(n,e,t){let r=t==="zephyr"?"sysbuild":"zephyr",o=n.get("SELECT prompt, defined_in FROM kconfig WHERE name = ? AND scope = ?",e,r);if(!o)return;let s=n.get("SELECT defined_in FROM kconfig WHERE name = ? AND scope = ?",e,t),i=l=>v(l,[]).map(d=>typeof d=="string"?d:d.file??""),c=new Set(i(s?.defined_in));if(i(o.defined_in).some(l=>c.has(l)))return;let a=String(o.prompt??"").trim();return`> **A different symbol shares this name.** \`${ne[r]}${e}\` exists in the ${r==="sysbuild"?"sysbuild":"application"} namespace and is declared elsewhere${a?`: "${a}"`:""}. ${r==="sysbuild"?"`sysbuild.conf`":"`prj.conf`"} takes that one.`}function Te(n){return n.map(e=>e.cond?`\`${e.value}\` if \`${e.cond}\``:`\`${e.value}\``)}var Et=n=>({name:"search_kconfig",title:"Search Kconfig symbols",description:'Search Zephyr Kconfig symbols by name or by what they do. Use this before writing any CONFIG_ line in prj.conf, a defconfig, or a Kconfig file. Symbol names change between Zephyr releases, so a name recalled from memory is often wrong. Accepts names with or without the CONFIG_ prefix, and plain-language queries such as "bluetooth peripheral role" or "spi dma". Returns each symbol with its type and prompt. Call get_kconfig for defaults and dependencies. A Zephyr tree has two Kconfig namespaces: pass scope="sysbuild", or write SB_CONFIG_, to search the sysbuild.conf options instead of the application ones.',inputSchema:{type:"object",properties:{query:{type:"string",description:"Symbol name fragment or plain-language description of the option."},scope:{type:"string",enum:["zephyr","sysbuild"],description:'Which Kconfig namespace to search. "zephyr" is prj.conf and CONFIG_ (the default); "sysbuild" is sysbuild.conf and SB_CONFIG_.'},limit:F(15)},required:["query"],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=j(e,"query"),r=A(e.limit,15),s=k(e,"scope")==="sysbuild"||/\bSB_CONFIG_/.test(t)?"sysbuild":"zephyr",i=ne[s],c=t.replace(/\bSB_CONFIG_/g,"").replace(/\bCONFIG_/g,""),a=n().search(`SELECT k.name, k.type, k.prompt, k.help, k.menu_path
         FROM kconfig_fts f JOIN kconfig k ON k.id = f.rowid
        WHERE kconfig_fts MATCH ? AND k.scope = ?
        ORDER BY bm25(kconfig_fts, 12.0, 4.0, 1.0)
        LIMIT ?`,c,[s,r],r);if(a.length===0)return L("Kconfig symbols",t,"Try a shorter query, a single keyword, or search the documentation with search_docs. Note that a symbol may exist only when a particular subsystem or board is selected.");let l=a.map(p=>({name:`${i}${String(p.name)}`,type:p.type??"unknown",prompt:p.prompt??"",summary:K(String(p.help??"").split(`

`)[0]??"",220),menuPath:p.menu_path??""})),d=l.map(p=>{let u=`### ${p.name}  \`${p.type}\``,h=p.prompt?`
${p.prompt}`:"",f=p.summary?`

${p.summary}`:"";return`${u}${h}${f}`}).join(`

`);return I(`Found ${l.length} Kconfig symbol(s) for "${t}".

${d}

Use get_kconfig for defaults, dependencies, and what selects a symbol.`,{query:t,count:l.length,results:l})}});function xt(n,e,t){let r=n.get("SELECT name, series, type, prompt, help, file, line FROM soc_kconfig WHERE name = ? ORDER BY series LIMIT 1",e);if(r&&t==="zephyr"){let a=n.all("SELECT DISTINCT series FROM soc_kconfig WHERE name = ? ORDER BY series",e).map(l=>String(l.series)).filter(l=>l!==String(r.series));return[`Kconfig symbol \`${ne[t]}${e}\` is declared under SoC series \`${String(r.series)}\`${a.length>0?` (also ${a.map(l=>`\`${l}\``).join(", ")})`:""}. This index cannot evaluate it. Zephyr sources a series' Kconfig only when that series is selected, and no SoC is selected here.`,"",`- **Type**: ${String(r.type||"unknown")}`,r.prompt?`- **Prompt**: ${String(r.prompt)}`:"",`- **Declared at**: \`${String(r.file)}:${String(r.line)}\``,r.help?`
${String(r.help).trim()}`:"","","_Parsed from the series Kconfig at declaration level, not evaluated. The type, prompt and location are accurate. Dependencies, defaults and what selects it are not available here: read the file with get_source, or build a project index for a board on this series, which resolves the symbol fully._"].filter((l,d,p)=>l!==""||d>0&&p[d-1]!=="").join(`
`)}let o=t==="zephyr"?"sysbuild":"zephyr";if(n.get("SELECT 1 FROM kconfig WHERE name = ? AND scope = ?",e,o))return`Kconfig symbol \`${ne[t]}${e}\` is not in the ${t==="sysbuild"?"sysbuild":"application"} namespace of the indexed Zephyr ${n.meta.zephyr_version??"unknown"} catalogue, but \`${ne[o]}${e}\` is. ${o==="sysbuild"?"`sysbuild.conf` is where that one is set.":"`prj.conf` is where that one is set."}`;let s=n.search(`SELECT k.name FROM kconfig_fts f JOIN kconfig k ON k.id = f.rowid
      WHERE kconfig_fts MATCH ? AND k.scope = ? ORDER BY bm25(kconfig_fts, 12.0, 4.0, 1.0) LIMIT 8`,e.replace(/_/g," "),[t],8),i=Ye((a,...l)=>n.all(a,...l),"SELECT name FROM kconfig WHERE name LIKE ? ESCAPE '\\' AND scope = '"+t+"' ORDER BY LENGTH(name) LIMIT 40",e,"name"),c=[...new Set([...s.map(a=>String(a.name)),...i])];return U("Kconfig symbol",`${ne[t]}${e}`,n.meta.zephyr_version??"unknown",c.map(a=>`${ne[t]}${a}`),"Generated, application-local, board/SoC-derived, and external-module symbols may not be covered.")+Zn(n,e)}function Zn(n,e){let t=e.split("_")[0]?.toLowerCase()??"";if(t.length<4)return"";let r=n.get(`SELECT series, dir FROM soc
      WHERE series IS NOT NULL AND series <> ''
        AND (LOWER(series) = ? OR LOWER(REPLACE(series, 'x', '')) = ? OR LOWER(name) LIKE ? || '%')
      ORDER BY LENGTH(series) LIMIT 1`,t,t,t);if(r){let s=String(r.series),i=n.get(`SELECT d.file AS file FROM kconfig_definition d
        WHERE d.file LIKE 'soc/%/' || ? || '/%' ORDER BY LENGTH(d.file) LIMIT 1`,s),c=String(i?.file??""),a=c?c.slice(0,c.lastIndexOf("/")):String(r.dir??"");return`

The name matches the indexed SoC series \`${s}\`. Symbols under a series are declared in that SoC's own Kconfig, which the tree sources only when an SoC is selected. This catalogue has no SoC selected, so such symbols are missing from the index rather than from Zephyr. `+(a?`Read \`${a}/Kconfig\` with get_source, `:"Read the SoC Kconfig with get_source, ")+"or build a project index for a board on that series so the symbol resolves here."}let o=n.get("SELECT name, dir FROM board WHERE LOWER(name) LIKE ? || '%' ORDER BY LENGTH(name) LIMIT 1",t);return o?`

The name matches the indexed board \`${String(o.name)}\`. Board-scoped symbols are declared in the board's own Kconfig, which is reachable with get_source under \`${String(o.dir)}\`.`:""}function Kn(n,e){let t;try{t=n.get("SELECT value, is_set FROM resolved_config WHERE name = ?",e)}catch{return}if(t)return Number(t.is_set)===1?`**In the ingested build:** \`${e}=${String(t.value)}\`.`:"**In the ingested build:** not set. If the application assigns it, the assignment did not take effect. Check the dependencies listed above."}function Un(n,e){let{name:t,scope:r,prefix:o}=St(e),s=`${o}${t}`,i=n.get("SELECT id, name, type, prompt, has_prompt, choice, n_defs FROM kconfig WHERE name = ? AND scope = ?",t,r);if(!i)return{key:s,text:`### ${s}

${xt(n,t,r)}`,structured:{name:s,found:!1,scope:r}};let c=n.get(`SELECT d.id, d.file, d.line, COALESCE(e.display, 'y') AS condition
       FROM kconfig_definition d
       LEFT JOIN kconfig_expr e ON e.id = d.condition_expr_id
      WHERE d.symbol_id = ? ORDER BY (d.prompt IS NULL), d.id LIMIT 1`,Number(i.id)),a=c?n.all(`SELECT v.display AS value, COALESCE(c.display, 'y') AS cond
             FROM kconfig_default d
             JOIN kconfig_expr v ON v.id = d.value_expr_id
             LEFT JOIN kconfig_expr c ON c.id = d.condition_expr_id
            WHERE d.definition_id = ? ORDER BY d.ord LIMIT 6`,Number(c.id)).map(g=>g.cond==="y"?`\`${String(g.value)}\``:`\`${String(g.value)}\` if \`${String(g.cond)}\``):[],l=n.all(`SELECT k2.name AS name
         FROM kconfig_choice_member m1
         JOIN kconfig_choice_member m2 ON m2.choice_id = m1.choice_id
         JOIN kconfig k1 ON k1.id = m1.symbol_id
         JOIN kconfig k2 ON k2.id = m2.symbol_id
        WHERE k1.name = ? AND k1.scope = ? AND k2.name <> k1.name ORDER BY k2.name`,t,r).map(g=>`${o}${String(g.name)}`),d=n.all("SELECT DISTINCT from_sym FROM kconfig_edge WHERE to_sym = ? AND kind = ? AND scope = ? ORDER BY from_sym LIMIT 8",t,"select",r).map(g=>`${o}${String(g.from_sym)}`),p=i.type??"unknown",u=Number(i.has_prompt)===1,h=c?String(c.condition):"y",f=Number(i.n_defs),w=O([`### ${s}`,`\`${p}\`${i.prompt?` \u2014 ${String(i.prompt)}`:""}`,Kn(n,t),vt(n,t,r),h!=="y"?`**Depends on:** \`${h}\``:void 0,a.length>0?`**Defaults:** ${a.join(", ")}`:void 0,u?void 0:"**Promptless** \u2014 do not assign this from `prj.conf`; enable a symbol that selects it.",d.length>0?`**Selected by:** ${d.join(", ")}`:void 0,l.length>0?`**Choice alternatives** (selecting one deselects the rest): ${l.join(", ")}`:void 0,f>1?`_${f} definition contexts \u2014 call get_kconfig with a single name for all of them._`:void 0]);return{key:s,text:w,structured:{name:s,found:!0,type:p,prompt:i.prompt??"",hasPrompt:u,dependsOn:h,definitionCount:f,selectedBy:d,choice:i.choice??null,choiceMembers:l,knowledgeLevel:"catalogue"}}}var Rt=n=>({name:"get_kconfig",title:"Get Kconfig symbols",description:'Get the full definition of a Zephyr Kconfig symbol: type, default values with the conditions that select them, what it depends on, what it selects and implies, valid ranges, help text, the files that define it, and which other symbols select or imply it. Use this to confirm a symbol exists before relying on it, and to diagnose a setting that appears to be ignored. Pass "names" to check many symbols in one call, which returns type, prompt, dependencies, defaults and choice alternatives for each; checking a whole prj.conf costs one call rather than one per line. The dependency information is catalogue-level. To confirm a setting is visible and took effect, read the resolved build configuration. Prefixes select the namespace: CONFIG_ is prj.conf, SB_CONFIG_ is sysbuild.conf. Ten symbol names exist in both namespaces with different meanings, so pass the prefix the file uses.',inputSchema:{type:"object",properties:{name:{type:"string",description:"Symbol name, with or without a prefix (e.g. CONFIG_BT_PERIPHERAL). An SB_CONFIG_ prefix selects the sysbuild namespace."},names:ee('Several symbols in one call, e.g. ["BT_PERIPHERAL", "NVS", "SETTINGS_NVS"]. Returns a compact summary of each instead of the full definition-context listing. Prefer this over repeated single-name calls.')},additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let{values:t,batched:r}=J(e,"name","names"),o=n();if(r)return X(t.map(y=>Un(o,y)),Q);let{name:s,scope:i,prefix:c}=St(t[0]),a=o.get(`SELECT id, name, type, prompt, help, has_prompt, choice, n_defs
         FROM kconfig WHERE name = ? AND scope = ?`,s,i);if(!a)throw new E(xt(o,s,i));let l=Number(a.n_defs),d=o.all(`SELECT d.id, d.file, d.line, d.prompt, d.menu_path, d.is_menuconfig,
              d.is_configdefault, COALESCE(e.display, 'y') AS condition,
              pe.display AS prompt_condition
         FROM kconfig_definition d
         LEFT JOIN kconfig_expr e ON e.id = d.condition_expr_id
         LEFT JOIN kconfig_expr pe ON pe.id = d.prompt_condition_id
        WHERE d.symbol_id = ? ORDER BY (d.prompt IS NULL), d.id
        LIMIT ?`,Number(a.id),zn),p=Math.max(0,l-d.length),u=d.map(y=>{let T=Number(y.id),x=o.all(`SELECT v.display AS value, COALESCE(c.display, 'y') AS cond
           FROM kconfig_default d
           JOIN kconfig_expr v ON v.id = d.value_expr_id
           LEFT JOIN kconfig_expr c ON c.id = d.condition_expr_id
          WHERE d.definition_id = ? ORDER BY d.ord`,T).map(N=>({value:String(N.value),...N.cond!=="y"?{cond:String(N.cond)}:{}})),_=N=>o.all(`SELECT r.target_name AS value, COALESCE(c.display, 'y') AS cond
             FROM kconfig_relation r
             LEFT JOIN kconfig_expr c ON c.id = r.condition_expr_id
            WHERE r.definition_id = ? AND r.kind = ? ORDER BY r.ord`,T,N).map(H=>({value:String(H.value),...H.cond!=="y"?{cond:String(H.cond)}:{}})),S=o.all(`SELECT low.display AS low, high.display AS high, COALESCE(c.display, 'y') AS cond
           FROM kconfig_range r
           JOIN kconfig_expr low ON low.id = r.low_expr_id
           JOIN kconfig_expr high ON high.id = r.high_expr_id
           LEFT JOIN kconfig_expr c ON c.id = r.condition_expr_id
          WHERE r.definition_id = ? ORDER BY r.ord`,T).map(N=>({low:String(N.low),high:String(N.high),...N.cond!=="y"?{cond:String(N.cond)}:{}}));return{file:String(y.file),line:Number(y.line),prompt:y.prompt??null,promptCondition:y.prompt_condition??null,menuPath:v(y.menu_path,[]),condition:String(y.condition),isMenuconfig:Number(y.is_menuconfig)===1,isConfigDefault:Number(y.is_configdefault)===1,defaults:x,selects:_("select"),implies:_("imply"),ranges:S}}),h=o.all("SELECT DISTINCT from_sym FROM kconfig_edge WHERE to_sym = ? AND kind = ? AND scope = ? ORDER BY from_sym LIMIT 40",s,"select",i).map(y=>String(y.from_sym)),f=o.all("SELECT DISTINCT from_sym FROM kconfig_edge WHERE to_sym = ? AND kind = ? AND scope = ? ORDER BY from_sym LIMIT 20",s,"imply",i).map(y=>String(y.from_sym)),w=o.all(`SELECT k2.name AS name, k2.prompt AS prompt, k2.type AS type
           FROM kconfig_choice_member m1
           JOIN kconfig_choice_member m2 ON m2.choice_id = m1.choice_id
           JOIN kconfig k1 ON k1.id = m1.symbol_id
           JOIN kconfig k2 ON k2.id = m2.symbol_id
          WHERE k1.name = ? AND k2.name <> k1.name
          ORDER BY k2.name`,s).map(y=>({name:`${c}${String(y.name)}`,prompt:y.prompt??null})),g=a.type??"unknown",$=`# ${c}${s}

\`${g}\`${a.prompt?` \u2014 ${String(a.prompt)}`:""}`,m=vt(o,s,i),b=O([$,m,a.help?String(a.help):void 0,u.length>0?`## Definition contexts (${u.length} of ${l}${p>0?", prompted contexts first":""})

`+u.map((y,T)=>O([`### Alternative ${T+1}: \`${y.file}:${y.line}\`${y.isConfigDefault?" (`configdefault`)":""}`,`**Depends on in this context:** \`${y.condition}\``,y.prompt?`**Prompt:** ${y.prompt}`:"**Prompt:** _none (not directly assignable from application configuration)_",y.promptCondition?`**Prompt visibility expression:** \`${y.promptCondition}\``:void 0,R("Defaults in this context",Te(y.defaults)),R("Selects in this context",Te(y.selects)),R("Implies in this context",Te(y.implies)),R("Ranges in this context",y.ranges.map(x=>`\`${x.low}\` .. \`${x.high}\`${x.cond?` if \`${x.cond}\``:""}`)),y.menuPath.length>0?`**Menu path:** ${y.menuPath.join(" > ")}`:void 0])).join(`

`)+(p>0?`

_${p} further definition context(s) are not shown; they are board, SoC, and shield defconfig alternatives. The effective value comes from the resolved build configuration, not from any single alternative here._`:""):void 0,R("Selected by (enabling any of these forces it on)",h.map(y=>`${c}${y}`)),R("Implied by",f.map(y=>`${c}${y}`)),a.choice?`**Part of choice** \`${String(a.choice)}\` \u2014 options are mutually exclusive.`:void 0,R("Alternatives in this choice (selecting one deselects the rest)",w.map(y=>`\`${y.name}\`${y.prompt?` \u2014 ${y.prompt}`:""}`)),Number(a.has_prompt)===0?"**Assignability:** promptless; do not assign this symbol from `prj.conf`.":"**Assignability:** at least one definition has a user-visible prompt. Visibility still depends on the selected build context."]);return I(b,{name:`${c}${s}`,found:!0,type:g,prompt:a.prompt??"",help:a.help??"",hasPrompt:Number(a.has_prompt)===1,definitions:u,definitionCount:l,definitionsTruncated:p>0,selectedBy:h.map(y=>`${c}${y}`),impliedBy:f.map(y=>`${c}${y}`),choice:a.choice??null,choiceMembers:w,knowledgeLevel:"catalogue"})}});var kt=n=>({name:"search_samples",title:"Search Zephyr samples",description:`Find Zephyr sample applications and upstream Twister test suites by what they demonstrate or verify. Each sample combines a prj.conf, devicetree overlays, and source, plus the Twister platforms upstream records for it. Adapting a sample is more reliable than assembling a configuration from documentation. Search here before writing a new application that uses an unfamiliar subsystem. Pass "board" without a query to list everything upstream declares for that board: the Twister platform lists and, separately, every suite that ships a configuration file named for one of the board's targets. Those files are the vendor's own settings for that exact target and are named in no Twister key, so they are invisible to any other search. Pass "kind" to restrict the results to samples or to tests.`,inputSchema:{type:"object",properties:{query:{type:"string",description:'What the sample should demonstrate, e.g. "ble peripheral", "lvgl display". Optional when "board" is given.'},board:{type:"string",description:"A board name or qualified target. With a query it ranks recorded integration or allowlist evidence for this board first; on its own it returns only what names it."},kind:{type:"string",enum:["sample","test"],description:'Restrict to worked examples under samples/ ("sample") or to upstream Twister suites under tests/ ("test"). Both are returned by default.'},limit:F(10)},additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=k(e,"query"),r=k(e,"board"),o=k(e,"kind"),s=A(e.limit,10);if(!t&&!r)throw new E('Supply "query", "board", or both.');let i=r?.split("/")[0],c=`s.path, s.name, s.description, s.tags, s.kind, s.scenarios,
              s.integration_platforms, s.platform_allow, s.files`,a=`(EXISTS (
           SELECT 1 FROM sample_platform p
            WHERE p.sample_id = s.id AND (p.platform = ? OR p.platform LIKE ?)
         ) OR EXISTS (
           SELECT 1 FROM sample_board_file bf WHERE bf.sample_id = s.id AND bf.board = ?
         ))`,l=o?" AND s.kind = ?":"",d=t?n().search(`SELECT ${c}
             FROM sample_fts f JOIN sample s ON s.id = f.rowid
            WHERE sample_fts MATCH ?${l}
            ORDER BY ${i?`CASE WHEN ${a} THEN 0 ELSE 1 END,`:""}
                     bm25(sample_fts, 6.0, 4.0, 3.0, 2.0)
            LIMIT ?`,t,[...o?[o]:[],...i?[i,`${i}/%`,i]:[],s],s):n().all(`SELECT ${c} FROM (
               SELECT s.*, ROW_NUMBER() OVER (PARTITION BY s.kind ORDER BY s.path) AS rank
                 FROM sample s
                WHERE ${a}${l}
             ) s
            ORDER BY s.rank, s.kind LIMIT ?`,i,`${i}/%`,i,...o?[o]:[],s);if(d.length===0)return L("samples or tests",t??String(r),t?'Try the subsystem name on its own ("bluetooth", "sensor", "usb"), or drop the board filter.':"No indexed sample or test names this board. Try the bare board name without a qualifier, or search by subsystem instead.");let p=m=>i!==void 0&&m.some(b=>b===i||b.startsWith(`${i}/`)),u=new Map;if(i)for(let m of n().all(`SELECT s.path AS suite, bf.path AS file, bf.target AS target
           FROM sample_board_file bf JOIN sample s ON s.id = bf.sample_id
          WHERE bf.board = ? ORDER BY s.path, bf.path`,i)){let b=String(m.suite),y={path:String(m.file),target:String(m.target??"")},T=u.get(b);T?T.push(y):u.set(b,[y])}let h=d.map(m=>{let b=v(m.integration_platforms,[]),y=v(m.platform_allow,[]);return{path:String(m.path),name:String(m.name),kind:String(m.kind),description:String(m.description??""),tags:v(m.tags,[]),scenarios:v(m.scenarios,[]),integrationPlatforms:b,platformAllow:y,files:v(m.files,[]),boardEvidence:r?[p(y)?"platform_allow":"",p(b)?"integration_platforms":"",u.has(String(m.path))?"a file under boards/":""].filter(Boolean):[],boardFiles:u.get(String(m.path))??[]}}),f=h.map(m=>[`### ${m.name}${m.kind==="test"?" _(upstream test suite)_":""}`,`\`${m.path}\``,m.description||"",m.tags.length>0?`tags: ${m.tags.join(", ")}`:"",m.scenarios.length>0?`twister scenarios: ${G(m.scenarios,4)}`:"",m.platformAllow.length>0?`platform_allow: ${G(m.platformAllow,6)}`:"",m.integrationPlatforms.length>0?`integration_platforms: ${G(m.integrationPlatforms,6)}`:"",m.boardEvidence.length>0?`names ${r} in: ${m.boardEvidence.join(", ")}`:"",m.boardFiles.length>0?`ships ${m.boardFiles.map(b=>`\`${b.path}\`${b.target?` for \`${b.target}\``:""}`).join(", ")} \u2014 upstream's own configuration for this target`:""].filter(Boolean).join(`
`)).join(`

`),w=i?n().all(`SELECT s.kind, COUNT(DISTINCT s.id) AS n FROM sample s
              WHERE ${a}${l}
              GROUP BY s.kind`,i,`${i}/%`,i,...o?[o]:[]).map(m=>({kind:String(m.kind),count:Number(m.n)})):[],g=w.reduce((m,b)=>m+b.count,0),$=i?`\`${i}\` is named by ${w.map(m=>`${m.count} ${m.kind}${m.count===1?"":"s"}`).join(" and ")}${g>h.length?`; showing ${h.length}. Raise "limit" or pass "kind" for the rest.`:"."}`:`Found ${h.length} sample(s) or test suite(s) for "${String(t)}".`;return I(`${$}

${f}

Use get_sample with a path to read its prj.conf, overlays, and source.`,{query:t??null,board:r??null,kind:o??null,count:h.length,totalsByKind:w,results:h})}}),Tt=n=>({name:"get_sample",title:"Read a Zephyr sample",description:"Read a sample application or an upstream Twister test suite: its description, the Twister platforms recorded for it, scenario names, and the contents of its prj.conf, devicetree overlays, CMakeLists.txt, and C sources. Use this to copy a working configuration instead of deriving one. The Kconfig and devicetree changes a subsystem needs are seldom documented in one place. It also shows what upstream tests on a given board. The index stores the small files under its size policy, not every file in the directory.",inputSchema:{type:"object",properties:{path:{type:"string",description:'Sample directory path, e.g. "samples/basic/blinky".'},files:{type:"array",items:{type:"string"},description:'Optional: only these files, relative to the sample directory (e.g. ["prj.conf"]). Defaults to configuration files plus sources.'},max_chars:{type:"integer",minimum:1e3,maximum:8e4,default:2e4,description:"Truncate the combined file contents at roughly this many characters."}},required:["path"],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=j(e,"path").replace(/\/+$/,""),r=Array.isArray(e.files)?e.files.filter(x=>typeof x=="string"):null,o=A(e.max_chars,2e4,8e4),s=n(),i=s.get(`SELECT id, path, kind, name, description, tags, scenarios, depends_on,
              integration_platforms, platform_allow, files, doc_path
         FROM sample WHERE path = ?`,t);if(!i){let x=t.replace(/[\\%_]/g,"\\$&"),_=s.all(`SELECT id, path, kind, name, description, tags, scenarios, depends_on,
                integration_platforms, platform_allow, files, doc_path
           FROM sample WHERE path LIKE ? ESCAPE '\\' ORDER BY LENGTH(path), path LIMIT 12`,`%/${x}`);if(_.length>1)throw new E(`Sample path "${t}" is ambiguous. Use one exact path:
`+_.map(S=>`- ${String(S.path)}`).join(`
`));i=_[0]}if(!i)return ie("Sample",t,s.meta.zephyr_version??"unknown",[],"Module-local samples and samples outside this index's declared roots may not be covered. Use search_samples to find an indexed path.");let c=Number(i.id),a=s.all("SELECT path, text FROM sample_file WHERE sample_id = ? ORDER BY path",c),l=x=>/^prj.*\.conf$/.test(x)?0:x.startsWith("boards/")?1:x.startsWith("src/")?3:2,d=a.filter(x=>!r||r.includes(String(x.path))).sort((x,_)=>l(String(x.path))-l(String(_.path))),p="",u=!1,h=[];for(let x of d){let _=String(x.path),S=`#### \`${_}\`
\`\`\`${ge(_)}
${String(x.text).trimEnd()}
\`\`\`

`;if(p.length+S.length>o){u=!0;continue}p+=S,h.push(_)}let f=v(i.integration_platforms,[]),w=v(i.platform_allow,[]),g=v(i.tags,[]),$=v(i.scenarios,[]),m=v(i.depends_on,[]),b=v(i.files,[]),y=String(i.kind??"sample"),T=O([`# ${String(i.name)}${y==="test"?" _(upstream test suite)_":""}`,i.description?String(i.description):void 0,`\`${String(i.path)}\``,R("Tags",g),$.length>0?`**Twister scenarios** \u2014 pass one to \`twister -s\`: ${G($,20)}`:void 0,R("Requires board support for",m),w.length>0?"**Twister platform allowlist** \u2014 a board must appear here to build this at all: "+G(w,30):void 0,f.length>0?"**Twister integration platforms** \u2014 the smaller set upstream CI runs: "+G(f,30):void 0,y==="test"?`**Run it:**
\`\`\`bash
west twister -T ${String(i.path)} -p <board-target>
\`\`\``:`**Build it:**
\`\`\`bash
west build -b <board-target> ${String(i.path)}
\`\`\``,p?`## Files

${p.trimEnd()}`:"_No file contents stored for this sample._",u?`_Some files omitted to stay within ${o} characters. All files: ${b.join(", ")}. Request specific ones with the "files" argument._`:void 0]);return I(T,{path:i.path,found:!0,kind:y,name:i.name,description:i.description??"",tags:g,scenarios:$,dependsOn:m,integrationPlatforms:f,platformAllow:w,filesIncluded:h,filesAvailable:b,truncated:u})}});import{spawnSync as Gn}from"node:child_process";import{existsSync as _e,readFileSync as Yn,realpathSync as Ne,statSync as It}from"node:fs";import{dirname as Ot,isAbsolute as de,join as we,relative as ye,resolve as Dt,sep as ue}from"node:path";var Ie=2e4,Nt=8e4,Mt=16*1024*1024;function Ce(n,e){let t=Gn("git",["-C",n,...e],{encoding:"utf8",maxBuffer:Mt,stdio:["ignore","pipe","ignore"]});return t.status===0?t.stdout:null}function Ct(n,e){let t=n.replace(/\\/g,"/").trim(),r=t;if(de(t)){if(!e)throw new E(`"${n}" is an absolute path, but the indexed Zephyr tree is not available on this machine, so it cannot be interpreted. Pass a tree-relative path such as "include/zephyr/drivers/gpio.h".`);let o=ye(e,Dt(t));if(o===""||o===".."||o.startsWith(`..${ue}`)||de(o))throw new E(`"${n}" is outside the indexed Zephyr tree.`);r=o.split(ue).join("/")}if(r=r.replace(/^\.\//,"").replace(/\/+$/,""),r==="")throw new E('The "path" argument must name a file in the Zephyr tree.');if(r.split("/").includes(".."))throw new E(`"${n}" must not contain ".." segments.`);return r}function Vn(n){let e=n;for(let t=0;t<40;t++){if(_e(we(e,".west","config")))return e;let r=Ot(e);if(r===e)break;e=r}return Ot(n)}var At=new Map;function Jn(n,e){let t=[];if(!e)return t;let r=At.get(e);if(r)return r;let o=Vn(e),s=[];try{s=n.all("SELECT name, path, revision FROM west_module WHERE path <> '' ORDER BY LENGTH(path) DESC, name")}catch{s=[]}for(let i of s){let c=String(i.path),a=we(o,c);_e(a)&&t.push({name:String(i.name),prefix:`${c.replace(/\/+$/,"")}/`,root:Ne(a),commit:String(i.revision??"")})}return t.push({name:"zephyr",prefix:"",root:e,commit:n.meta.zephyr_commit??n.descriptor.zephyrCommit}),At.set(e,t),t}var jt=new Map;function Xn(n,e){let t=`${n??""}\0${e}`,r=jt.get(t);if(r!==void 0)return r;let s=(n?Ce(n,["remote","get-url","origin"])?.trim():null)?.match(/[:/]([^/:]+)\/([^/]+?)(?:\.git)?\/?$/),i=s?`${s[1]}/${s[2]}`:e==="pinned-upstream"?"zephyrproject-rtos/zephyr":null;return jt.set(t,i),i}function Oe(n,e,t,r){let o=r?`#L${r.start}-L${r.end}`:"";return`${n?`${n}@`:""}${e.slice(0,8)}:${t}${o}`}var Lt=n=>({name:"get_source",title:"Read Zephyr source at the indexed revision",description:'Read a file from the indexed Zephyr tree, or from any west module beside it, at the revision the index was built from. Use it when a symbol lookup is not enough and you need the source: a board .dts, an SoC Kconfig, a driver implementation, a linker script, a runner script, or a vendor HAL header. A HAL header answers whether the registers a driver uses exist on your part. Name a module file by its workspace-relative path, for example "modules/hal/stm32/stm32cube/...". Try get_binding, get_kconfig, and get_api first; they are faster and more precise. Use this tool for what they do not cover, rather than fetching the file from GitHub, where the default branch is a different Zephyr version.',inputSchema:{type:"object",properties:{path:{type:"string",description:'Path relative to the Zephyr tree root, e.g. "boards/st/nucleo_f401re/nucleo_f401re.dts" or "include/zephyr/drivers/gpio.h". A module file is named by its workspace-relative path, e.g. "modules/hal/stm32/stm32cube/stm32n6xx/soc/stm32n657xx.h". An absolute path inside any of those trees is also accepted.'},start:{type:"integer",minimum:1,description:"First line to return, 1-based and inclusive. Defaults to the start of the file."},end:{type:"integer",minimum:1,description:"Last line to return, 1-based and inclusive. Defaults to the end of the file."},max_chars:{type:"integer",minimum:1,maximum:Nt,default:Ie,description:`Truncate the returned content at roughly this many characters (default ${Ie}).`}},required:["path"],additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let t=n(),r=t.meta.source_path??t.descriptor.zephyrRoot,o=r&&_e(r)?Ne(r):null,s=j(e,"path"),i=Jn(t,o),c=i.find(D=>D.prefix==="")??null,a;if(de(s.replace(/\\/g,"/").trim())){let D=Dt(s.replace(/\\/g,"/").trim()),C=i.find(M=>{let re=ye(M.root,D);return re!==""&&re!==".."&&!re.startsWith(`..${ue}`)&&!de(re)});C?(c=C,a=ye(C.root,D).split(ue).join("/")):a=Ct(s,o)}else{let D=Ct(s,o),C=i.find(M=>M.prefix!==""&&D.startsWith(M.prefix)&&_e(we(M.root,D.slice(M.prefix.length))));C?(c=C,a=D.slice(C.prefix.length)):a=D}let l=c?.root??null,d=c?.commit||t.descriptor.zephyrCommit,p=!!(c&&c.prefix!==""),u=p?`${c.prefix}${a}`:a,h=Xn(l,p?"module":t.descriptor.sourceKind),f=A(e.max_chars,Ie,Nt),w=e.start===void 0?void 0:A(e.start,1,1e6),g=e.end===void 0?void 0:A(e.end,1,1e6);if(w!==void 0&&g!==void 0&&g<w)throw new E(`"end" (${g}) is before "start" (${w}).`);if(!l)return I(`The indexed Zephyr tree is not present on this machine, so its files cannot be read here.

Use this reference for the exact revision:

\`${Oe(h,d,u,null)}\`

_Indexed Zephyr ${t.meta.zephyr_version??"unknown"} at commit \`${d}\`. A file fetched from a default branch is a different Zephyr version._`,{path:u,found:!1,treeAvailable:!1,commit:d,reference:Oe(h,d,u,null)});let $=Ce(l,["cat-file","-t",`${d}:${a}`])?.trim(),m=null,b=!1;if($==="blob")m=Ce(l,["cat-file","-p",`${d}:${a}`]),b=m!==null;else if($==="tree")throw new E(`"${a}" is a directory in the indexed tree, not a file. Name a file inside it.`);if(m===null){let D=we(l,a),C;try{C=Ne(D)}catch{let re=p?`the ${c.name} module tree`:`the indexed Zephyr ${t.meta.zephyr_version??""} tree`,he=i.filter($e=>$e.prefix!=="");throw new E(`"${u}" does not exist in ${re}. Check the path against search_boards, search_samples, or the header reported by get_api.`+(he.length>0&&!p?` Module sources are readable too, under their workspace-relative paths \u2014 ${he.slice(0,4).map($e=>`\`${$e.prefix}\``).join(", ")}${he.length>4?`, and ${he.length-4} more`:""}.`:""))}let M=ye(l,C);if(M===".."||M.startsWith(`..${ue}`)||de(M))throw new E(`"${u}" resolves outside the tree it names.`);if(!It(C).isFile())throw new E(`"${u}" is not a regular file in the indexed tree.`);if(It(C).size>Mt)throw new E(`"${u}" is too large to return. Request a line range with "start" and "end".`);m=Yn(C,"utf8")}if(m.includes("\0"))throw new E(`"${u}" is a binary file and has no useful text representation.`);let y=m.split(`
`);y.length>1&&y[y.length-1]===""&&y.pop();let T=y.length,x=Math.min(w??1,T),_=Math.min(g??T,T);if(w!==void 0&&w>T)throw new E(`"${u}" has ${T} lines; "start" (${w}) is past the end.`);let S=y.slice(x-1,_),N=S.join(`
`),H=null;if(N.length>f){let D=0,C=[];for(let M of S){if(D+M.length+1>f)break;C.push(M),D+=M.length+1}N=C.join(`
`),H=x+C.length-1}let q=H??_,se=Oe(h,d,u,{start:x,end:q}),pe=x===1&&q===T?`${T} lines`:`lines ${x}\u2013${q} of ${T}`,Zt=p?`_Read from the \`${c.name}\` module`+(d?` at manifest revision \`${d.slice(0,8)}\``:"")+", not from the Zephyr tree._":"",Kt=[`# \`${u}\``,"",`\`\`\`${ge(a)}`,N,"```","",H!==null?`_Truncated at ${f} characters. Request the rest with \`start: ${q+1}\`._`:"",`_\`${se}\` \u2014 ${pe}._`,Zt,b?`_Read from pinned commit \`${d}\`, not from the working tree._`:`_Read from the tree on disk. This content could not be verified against pinned commit \`${d}\`. Call index_status to check whether the tree has changed._`].filter((D,C,M)=>D!==""||C>0&&M[C-1]!=="").join(`
`);return I(Kt,{path:u,found:!0,treeAvailable:!0,commit:d,tree:c?.name??"zephyr",inModule:p,reference:se,start:x,end:q,totalLines:T,truncated:H!==null,verifiedAtCommit:b,content:N})}});import{createHash as Ht}from"node:crypto";import{existsSync as ar,readFileSync as je,readdirSync as cr,realpathSync as lr,statSync as dr}from"node:fs";import{basename as ur,join as oe,resolve as De}from"node:path";import{spawnSync as pr}from"node:child_process";import{createHash as Qn}from"node:crypto";import{existsSync as er,lstatSync as tr,readFileSync as nr,readlinkSync as rr,realpathSync as ir}from"node:fs";import{join as or}from"node:path";import{spawnSync as sr}from"node:child_process";function Ae(n,e){let t=sr("git",["-C",n,...e],{encoding:"utf8",maxBuffer:268435456,stdio:["ignore","pipe","ignore"]});return t.status===0?t.stdout.trim():null}function Ft(n){let e=ir(n),t=Ae(e,["rev-parse","HEAD"]);if(!t)return null;let r=Ae(e,["diff","--binary","HEAD"])??"",o=(Ae(e,["ls-files","--others","--exclude-standard"])??"").split(`
`).filter(s=>!!s&&s!==".zephyr-ai-managed.json").sort().map(s=>{let i=or(e,s);if(!er(i))return{path:s,missing:!0};try{let c=tr(i);return c.isSymbolicLink()?{path:s,symlink:rr(i)}:c.isFile()?{path:s,sha256:Qn("sha256").update(nr(i)).digest("hex")}:{path:s,special:c.mode}}catch{return{path:s,unreadable:!0}}});return{commit:t,dirty:!!(r||o.length),stateFingerprint:me({commit:t,diff:r,untracked:o})}}var hr={explicit:"explicitly selected via ZEPHYR_AI_INDEX",project:"the active fingerprinted index for this project",development:"the repository development index"};function mr(n){try{let e=je(oe(n,"VERSION"),"utf8"),t=s=>e.match(new RegExp(`^${s}\\s*=\\s*(.*)$`,"m"))?.[1]?.trim()??"",r=[t("VERSION_MAJOR"),t("VERSION_MINOR"),t("PATCHLEVEL")].join("."),o=t("EXTRAVERSION");return o?`${r}-${o}`:r}catch{return null}}function fr(n){let e=pr("west",["manifest","--freeze"],{cwd:n,encoding:"utf8",stdio:["ignore","pipe","ignore"]});if(e.status===0&&e.stdout.trim())return Ht("sha256").update(e.stdout).digest("hex");let t="",r="west.yml";try{let s=je(oe(n,".west","config"),"utf8");t=s.match(/^\s*path\s*=\s*(.+)$/m)?.[1]?.trim()??"",r=s.match(/^\s*file\s*=\s*(.+)$/m)?.[1]?.trim()??r}catch{}let o=[...t?[oe(n,t,r)]:[],oe(n,"west.yml"),oe(n,"west.yaml")].find(ar);return o?Ht("sha256").update(je(o)).digest("hex"):null}function gr(){let n=process.env.ZEPHYR_AI_PLUGIN_DATA??process.env.CLAUDE_PLUGIN_DATA;if(!n)return{bytes:0,files:0};let e=De(n,"indexes"),t=0,r=0,o=s=>{let i;try{i=cr(s,{withFileTypes:!0})}catch{return}for(let c of i){let a=oe(s,c.name);c.isDirectory()?o(a):c.isFile()&&c.name==="zephyr.db"&&(t+=dr(a).size,r++)}};return o(e),{bytes:t,files:r}}function Pt(n){try{return lr(De(n))}catch{return De(n)}}var Bt=n=>({name:"index_status",title:"Index status",description:"Report which Zephyr version is indexed, where the index came from, and what it covers. Call this when an answer looks wrong for the version in use, when a symbol or board that should exist is missing, or at the start of work on an unfamiliar project. It also detects whether the project has a west workspace pinned to a different Zephyr version than the index, which is the common reason advice does not compile.",inputSchema:{type:"object",properties:{},additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:()=>{let e=n(),t=e.meta,r=[["Documentation pages",t.count_docs??"?"],["Documentation sections",t.count_doc_chunks??"?"],["Kconfig symbols",t.count_kconfig??"?"],["Devicetree compatibles",t.count_bindings??"?"],["Devicetree properties",t.count_dt_properties??"?"],["Boards",t.count_boards??"?"],["Board build targets",t.count_board_targets??"?"],["SoCs",t.count_socs??"?"],["Samples",t.count_samples??"?"],["C API symbols",t.count_api??"?"],["West flash/debug runners",t.count_runners??"?"],["Board-runner pairings",t.count_board_runners??"?"],["West commands",t.count_west_commands??"?"]],o=e.info.projectRoot??process.env.ZEPHYR_AI_PROJECT_ROOT??process.env.CLAUDE_PROJECT_DIR,s=Ke(o??process.cwd()),i=s?Ue(s):null,c=i?mr(i):null,a=i?Ft(i):null,l=a?.commit??null,d=a?.stateFingerprint??null,p=t.zephyr_version??"unknown",u=e.descriptor.zephyrCommit,h=c!==null&&c!==p,f=l!==null&&l!==u,w=d!==null&&d!==e.descriptor.zephyrTreeFingerprint,g=h||f||w,$=e.descriptor.projectRoot?!!(o&&Pt(o)===Pt(e.descriptor.projectRoot)):null,m=s?fr(s):null,b=e.descriptor.westManifestHash?m===null?null:m===e.descriptor.westManifestHash:null,y=gr(),T=e.descriptor.boardTarget||e.descriptor.buildDirectory?"catalogue context with build identity recorded (resolved values are not ingested)":"catalogue context",x=O([`# Zephyr index: version ${p}`,`Source: ${hr[e.info.origin]??e.info.origin}
Artifact: \`${ur(e.info.path)}\` (${(e.sizeBytes/1024/1024).toFixed(1)} MiB)
Context: ${T}`+(u?`
Commit: \`${u}\``:"")+`
Fingerprint: \`${e.descriptor.contextFingerprint}\``+(t.built_at?`
Built: ${t.built_at}`:""),R("Reproducibility",[...t.input_hash?[`inputs: \`${t.input_hash}\` covers the declared inputs. If this differs between two machines, they read different files, tool versions, or environment. Compare those first.`]:[],...t.content_hash?[`content: \`${t.content_hash}\` covers the stored rows. Matching inputs with differing content means the indexer is not deterministic, not that the source differs.`]:[]]),R("Indexed rows",r.map(([_,S])=>`${_}: ${S}`)),R("Coverage",Object.entries(e.descriptor.coverage).sort(([_],[S])=>ae(_,S)).map(([_,S])=>`${_}: ${S.complete?"complete":"incomplete"}`+(S.note?` \u2014 ${S.note}`:""))),e.descriptor.producer?R("Built by",[`Node ${e.descriptor.producer.node} \xB7 SQLite ${e.descriptor.producer.sqlite}`+(e.descriptor.producer.python?` \xB7 ${e.descriptor.producer.python}`:"")+(e.descriptor.producer.doxygen?` \xB7 Doxygen ${e.descriptor.producer.doxygen}`:""),`collator: ${e.descriptor.producer.collator} \u2014 recorded because ordering must not depend on it`]):void 0,"Coverage describes the indexed Zephyr tree and the modules named when it was built. It never describes this project's own Kconfig or devicetree bindings, which are outside the index, so a symbol or compatible missing here may still be valid in this workspace.",s?g?`## \u26A0\uFE0F Source-context mismatch

This project is a west workspace whose Zephyr is **${c}** at commit \`${l??"unknown"}\`, but the index describes **${p}** at commit \`${u}\`.

`+(w?`The checkout content also differs from the indexed tree (tracked or untracked changes are present).

`:"")+"Kconfig symbol names, devicetree properties, and APIs move between releases, so answers from this index may not apply. Rebuild it against this workspace by invoking the `zephyr-index` skill.":`**West workspace** detected; Zephyr ${c??"unknown"} at the same commit as the index.`:"_No west workspace detected for the active project; this answer is catalogue-scoped._",`**Stored indexes:** ${y.files} artifact(s), ${(y.bytes/1024/1024).toFixed(1)} MiB total.`,b===!1?"**Manifest mismatch:** the active west manifest differs from the index descriptor; rebuild the project index.":void 0]);return I(x,{zephyrVersion:p,zephyrCommit:t.zephyr_commit??null,origin:e.info.origin,sizeBytes:e.sizeBytes,builtAt:t.built_at??null,docBaseUrl:t.doc_base_url??null,counts:Object.fromEntries(r),workspaceDetected:!!s,workspaceZephyrVersion:c,workspaceZephyrCommit:l,workspaceTreeFingerprint:d,treeMismatch:w,versionMismatch:h,commitMismatch:f,sourceContextMismatch:g,projectMatch:$,manifestMatch:b,descriptor:Pe(e.descriptor),storedIndexUsage:y})}});function br(n){let e={dev_id:"`--dev-id` to pick between several attached probes",mult_dev_ids:"`--dev-id` more than once, to flash several devices",flash_addr:"`--dt-flash` to flash at an address from devicetree",erase:"`--erase` for a mass erase before flashing",reset:"`--reset` to reset after flashing",reset_types:"`--reset-type` to choose how the device is reset",extload:"`--extload` for an external loader",tool_opt:"`-O`/`--tool-opt` to pass options straight to the underlying tool",file:"`--file` to override the artefact chosen from the build directory",rtt:"`west rtt` for a Segger RTT terminal"},t=[];for(let[o,s]of Object.entries(e))n[o]===!0&&t.push(s);let r=n.reset_types_supported;return Array.isArray(r)&&r.length>0&&t.push(`\`--reset-type\` accepts: ${r.map(o=>`\`${String(o)}\``).join(", ")}`),t}function yr(n,e){let t=n.get(`SELECT COUNT(*) AS boards,
            SUM(flash_default) AS flash_default_for,
            SUM(debug_default) AS debug_default_for
       FROM board_runner WHERE runner = ? AND available = 1`,e),r=n.all(`SELECT b.name FROM board_runner r JOIN board b ON b.id = r.board_id
      WHERE r.runner = ? AND r.available = 1 ORDER BY b.name LIMIT 6`,e);return{boards:t?Number(t.boards??0):0,flashDefaultFor:t?Number(t.flash_default_for??0):0,debugDefaultFor:t?Number(t.debug_default_for??0):0,examples:r.map(o=>String(o.name))}}function _r(n,e){let t=n.get("SELECT name, module, description, capabilities, commands FROM runner WHERE name = ?",e);if(!t){let a=n.all("SELECT name FROM runner ORDER BY name").map(l=>String(l.name));return{key:e,text:`## ${e}

${U("Runner",e,n.meta.zephyr_version??"unknown",a,"Runners come from this Zephyr tree; a runner added by an out-of-tree module is not covered.")}`,structured:{name:e,found:!1}}}let r=v(t.capabilities,{}),o=v(t.commands,[]),s=yr(n,e),i=t.description??"",c=O([`## \`${e}\``,i,o.length>0?`**Supports:** ${o.map(a=>`\`west ${a}\``).join(", ")}`:"**Supports no west command directly.** It is selected for a board but drives nothing through `west flash` or `west debug`.",R("Options this runner accepts",br(r)),s.boards>0?`**Selected by ${s.boards} board(s)** in this tree`+(s.flashDefaultFor>0?`, the flash default for ${s.flashDefaultFor}`:"")+(s.debugDefaultFor>0?`, the debug default for ${s.debugDefaultFor}`:"")+`.
For example: ${G(s.examples,6)}`:"**No board in this tree registers this runner.**",`_Implementation: \`${String(t.module)}\`. Read it with get_source._`]);return{key:e,text:c,structured:{name:e,found:!0,module:t.module,description:i,commands:o,capabilities:r,usedBy:s}}}var Wt=n=>({name:"get_runner",title:"Get a west runner",description:"Get what a Zephyr flash or debug runner supports in this Zephyr version: which west commands it implements, which options it accepts, and which boards select it. Use it before passing `west flash -r <runner>` or an option such as `--dev-id`, `--erase`, or `--reset-type`. Runners accept different options, and west rejects one the runner does not declare. Capabilities come from the runner classes in the indexed tree, not from a fixed list. Accepts one name or a list.",inputSchema:{type:"object",properties:{name:{...Ve,description:'Runner name, e.g. "openocd", "jlink", "esp32".'},names:ee("Several runner names, answered compactly in one call.")},additionalProperties:!1},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},handler:e=>{let{values:t,batched:r}=J(e,"name","names"),o=n(),s=t.map(i=>_r(o,i));if(!r){let i=s[0];return{content:[{type:"text",text:i.text}],structuredContent:i.structured}}return X(s,Q)}});var wr=[ht,_t,Et,Rt,at,pt,rt,it,Wt,Xe,et,kt,Tt,wt,$t,Lt,Bt];function qt(n){return wr.map(e=>e(n))}import{fileURLToPath as Sr}from"node:url";import{statSync as vr}from"node:fs";var zt={name:"@zephyr-ai/mcp-server",version:"0.9.0",private:!0,type:"module",description:"MCP server (protocol 2025-11-25, stdio) serving the Zephyr knowledge index",license:"Apache-2.0",scripts:{build:"esbuild src/main.ts --bundle --platform=node --target=node24 --format=esm --minify --legal-comments=none --outfile=../../plugin/mcp/zephyr-mcp.mjs","build:dev":"esbuild src/main.ts --bundle --platform=node --target=node24 --format=esm --sourcemap --outfile=dist/main.mjs",pretest:"esbuild test/*.test.ts --bundle --platform=node --target=node24 --format=esm --outdir=dist-test --out-extension:.js=.mjs",test:'node --test "dist-test/*.test.mjs"'},dependencies:{}};var Er=zt.version,xr=["Zephyr RTOS reference, indexed from one specific Zephyr revision.","","Load and call these tools before writing or editing a prj.conf, a devicetree overlay, a board","target, a runner argument, or a driver binding \u2014 not after a build fails. They are the only","source here that matches the Zephyr revision this project actually builds against; a web","search and a repository default branch return a different version.","","Query this server before writing Zephyr code, not after a build fails. Four common errors","each have a tool that prevents them:","- CONFIG_ symbol that does not exist -> search_kconfig / get_kconfig before editing prj.conf.","- Devicetree property that does not exist -> get_binding before editing a .dts or .overlay.","  Bindings inherit most properties through include: chains, so reading the binding file is","  not enough.","- Wrong board target -> search_boards. Targets are qualified, e.g. esp32s3_devkitc/esp32s3/procpu.","- Wrong flash or debug runner -> get_board lists the runners a board registers and which one","  each command selects. get_runner says what a runner accepts. west flash and west debug do","  not always select the same runner, and west rejects an option the runner does not declare.","","Batch lookups. get_kconfig, get_binding and get_api each accept a list and answer all of it","in one call, so checking a whole prj.conf or overlay costs one call. check_config takes the","file contents and returns a verdict per line. A shell loop over the tree returns yes or no;","these tools return the type, prompt, dependencies and defaults as well.","","Use get_source when you need a file rather than a symbol: a board .dts, an SoC Kconfig, a","driver, a linker script, a runner script, or a vendor HAL header from a west module. It reads","the file at the revision this index was built from. A web search or a repository default","branch returns a different Zephyr version.","","Two questions have an answer here and nowhere else, because they are aggregates over the whole","tree rather than facts in one file:","- A part answers 0x19 from its WHO_AM_I or chip-ID register: which driver accepts it? Pass","  identity_value to search_bindings. get_binding reports the same contract forwards, and a","  driver whose identity check the part fails initialises and returns numbers that are not","  readings.","- What does upstream already publish for this board target? get_board and search_samples name","  every sample and test that ships a configuration file for it, which is where the vendor put","  the DMA channels, clock sources and cache attributes for each peripheral.","","Call index_status if answers look wrong for the project. It reports the indexed Zephyr version","and detects a west workspace pinned to a different one."].join(`
`);function Rr(){let n=null,e,t=()=>{let i;try{i=Ze({...process.env,...e?{ZEPHYR_AI_PROJECT_ROOT:e}:{}})}catch(c){let a=c instanceof B?c.message:"Index discovery failed.";throw new E(a)}if(n){if(i&&i.path===n.info.path&&i.identity===n.info.identity)return n;n.close(),n=null}if(!i)throw new E("No Zephyr index is available, so the lookup tools cannot answer. Build one with the `zephyr-index` skill: it locates the project's Zephyr tree and runs the bundled indexer. To use an existing index instead, set ZEPHYR_AI_INDEX to its path. ZEPHYR_AI_INDEX is the only variable that selects an index. Setting ZEPHYR_AI_PROJECT_ROOT does not work: the plugin sets it from ${CLAUDE_PROJECT_DIR}, and the CLI overwrites that with the session working directory.");try{n=new fe(i)}catch(c){throw V.log(`failed to open selected index: ${c.message}`),new E("The selected Zephyr index is incompatible or corrupt. Run the zephyr-index skill to replace it.")}return V.log(`serving Zephyr ${n.meta.zephyr_version??"?"} (${i.origin}, ${n.descriptor.contextFingerprint.slice(0,12)})`),n},r=new V({name:"zephyr",version:Er,title:"Zephyr RTOS",description:"Version-exact Zephyr RTOS reference: Kconfig, devicetree, boards, API, samples.",instructions:xr,rootsChanged:i=>{e=i.flatMap(c=>{try{if(!c.startsWith("file://"))return[];let a=Sr(c);return vr(a).isDirectory()?[a]:[]}catch{return[]}})[0]}}),o=qt(t);for(let i of o)r.tool(i);let s=o.find(i=>i.name==="index_status");r.resource({uri:"zephyr://index/status",name:"zephyr-index-status",title:"Zephyr index status",description:"Which Zephyr version is indexed, where it came from, and what it covers.",mimeType:"text/markdown",read:async()=>(s?await s.handler({}):void 0)?.content[0]?.text??"Index status unavailable."}),process.on("uncaughtException",i=>{V.log(`uncaught exception: ${i.stack??i.message}`),process.exit(1)}),process.on("unhandledRejection",i=>{V.log(`unhandled rejection: ${i instanceof Error?i.stack??i.message:String(i)}`),process.exit(1)}),r.start()}Rr();
