name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  quick:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
      - run: npm ci
      - run: npm install -g @anthropic-ai/claude-code
      - run: npm run check:quick

  contract:
    # Proves scripts/toolchain.json is complete. A hosted runner image and a
    # developer machine both carry far more than the contract declares, so a check
    # that reaches for an undeclared binary passes in both. This job has only what
    # the contract names, which makes it the one place that catches that.
    runs-on: ubuntu-latest
    container: node:24-trixie-slim
    steps:
      # git and CA certificates are what the container needs to fetch the repo at
      # all. Everything past this point must come from scripts/toolchain.json.
      - run: apt-get update && apt-get install --yes --no-install-recommends git ca-certificates python3
      - uses: actions/checkout@v7
      - run: npm ci
      - run: npm install -g @anthropic-ai/claude-code
      - run: npm run check:quick

  full-corpus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
      - uses: actions/cache@v6
        with:
          path: .cache/zephyr
          key: zephyr-${{ runner.os }}-node-24-schema-5-${{ hashFiles('zephyr.lock.json', 'package-lock.json') }}
      - run: npm ci
      - run: npm install -g @anthropic-ai/claude-code
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
      - run: npm run fetch:zephyr
      - run: python -m pip install -r .cache/zephyr/scripts/requirements-base.txt
      - run: npm run check

  extended-toolchain:
    # Release gate. Manual runs stay available; a tagged build cannot skip it.
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
      - uses: actions/cache@v6
        with:
          path: .cache
          key: zephyr-west-${{ runner.os }}-node-24-schema-5-${{ hashFiles('zephyr.lock.json', 'package-lock.json') }}
      - name: Install semantic and build prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install --yes cmake ninja-build doxygen device-tree-compiler gperf ccache
          npm run fetch:zephyr
          python -m pip install -r .cache/zephyr/scripts/requirements-base.txt
          npm run fetch:modules
          # Zephyr's Espressif SoC CMakeLists requires esptool on PATH before it
          # will configure, and requirements-base.txt does not carry it.
          python -m pip install "esptool>=5.0.2"
      - name: Install and verify pinned Zephyr SDK
        env:
          SDK_VERSION: 1.0.1
        run: |
          archive="zephyr-sdk-${SDK_VERSION}_linux-x86_64_gnu.tar.xz"
          url="https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v${SDK_VERSION}"
          curl --fail --location --output "$RUNNER_TEMP/$archive" "$url/$archive"
          curl --fail --location --output "$RUNNER_TEMP/sha256.sum" "$url/sha256.sum"
          cd "$RUNNER_TEMP"
          sha256sum --check --ignore-missing sha256.sum
          tar -xf "$archive"
          "$RUNNER_TEMP/zephyr-sdk-${SDK_VERSION}/setup.sh" -t all -h -c
          echo "ZEPHYR_TOOLCHAIN_VARIANT=zephyr" >> "$GITHUB_ENV"
          echo "ZEPHYR_SDK_INSTALL_DIR=$RUNNER_TEMP/zephyr-sdk-${SDK_VERSION}" >> "$GITHUB_ENV"
      - run: npm ci
      - run: npm install -g @anthropic-ai/claude-code
      - run: npm run check:release
